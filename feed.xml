<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://ottersem.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://ottersem.github.io/" rel="alternate" type="text/html" hreflang="ko"/><updated>2025-02-01T16:27:55+00:00</updated><id>https://ottersem.github.io/feed.xml</id><title type="html">Techlog</title><subtitle>CV and Blog about Machine Learning, Computer Vision, Data Science, Python. </subtitle><entry><title type="html">Initial value of weight</title><link href="https://ottersem.github.io/blog/2024/Initial-value-of-weight/" rel="alternate" type="text/html" title="Initial value of weight"/><published>2024-10-07T21:29:00+00:00</published><updated>2024-10-07T21:29:00+00:00</updated><id>https://ottersem.github.io/blog/2024/Initial-value-of-weight</id><content type="html" xml:base="https://ottersem.github.io/blog/2024/Initial-value-of-weight/"><![CDATA[<h1 id="가중치의-초깃값">가중치의 초깃값</h1> <h2 id="가중치-감소weight-decay">가중치 감소weight decay</h2> <p>가중치 감소는 가중치 매개변수의 값이 작아지도록 학습하여 오버피팅이 일어나지 않게 하는 것이다. 만약 가중치를 모두 0 (또는 균일한 값)으로 설정하면 오차역전파법에서 모든 가중치의 값이 똑같이 갱신되기에 학습이 올바로 이뤄지지 않는다. 만약 가중치가 모두 0이라면 순전파 때는 입력층의 가중치가 0이기 때문에 다음 뉴런에 모두 같은 값이 전달되며 이는 역전파 때 두 번째 층의 가중치가 모두 똑같이 갱신된다는 말이다. 따라서 가중치가 고르게 되어버리는 (가중치의 대칭적인 구조를 무너뜨리려면) 초깃값을 무작위로 설정해야한다.</p> <h2 id="활성화-값-분포">활성화 값 분포</h2> <p>은닉층의 활성화값의 분포를 관찰하면 중요한 정보를 얻을 수 있다. 무작위로 생성한 입력 데이터에 표준편차가 1인 정규분포를 이용하는 시그모이드 함수를 이용하면, 각 층의 활성화값들이 0과 1에 치우쳐진 U자 형태를 이룬다. 여기에서 사용한 시그모이드 함수는 출력이 0 또는 1에 가까워지면 그 미분은 0에 다가간다. 따라서 역전파의 기울기 값이 점점 작아지다가 사라지며 이를 <strong>기울기 소실Gradient Vanishing</strong>라고 한다. 만약 가중치의 표준편차를 0.01로 바꾸면 0.5 부근에 활성화값이 집중되는데, 이는 기울기 소실 문제를 발생시키지는 않지만, 표현력 관점에서는 큰 문제라고 볼 수 있다. 다수의 뉴런이 거의 같은 값을 출력하고 있으니 뉴런을 여러개 둔 의미가 없어진다는 의미이다. 정리하자면 뉴런 100개가 거의 같은 값을 출력한다면 뉴런 1개짜리와 별반 다를게 없다는 것이다. 따라서 활성화값들이 치우치면 <strong>표현력을 제한</strong>한다는 관점에서 문제가 된다.</p> <h3 id="xavier-초기값">Xavier 초기값</h3> <p>자비에르 글로로트와 요슈아 벤지오는 각 층의 활성화값들을 광범위하게 분포시킬 목적으로 가중치의 적절한 분포를 찾고자 했다. 그리고 앞 계층의 노드가 $n$개라면 표준편차가 $\frac{1}{\sqrt n}$인 분포를 사용하면 된다는 결론을 이끌었고, 이 초기값을 <strong>Xavier 초기값</strong>이라고 한다 Xavier 초기값을 이용하면 층이 깊어지면서 형태가 다소 일그러지지만 뒤집어진 U자의 형태를 이루며 이전의 방식보다는 확실하게 넓게 분포됨을 알 수 있다. 또한 각 층에 흐르는 데이터는 적당히 퍼져 있으므로, 시그모이드 함수의 표현력도 제한받지 않고 학습이 효율적으로 이뤄지는 것을 기대할 수 있다.</p> <h3 id="tanh-함수">tanh 함수</h3> <p>Xavier 초기값을 이용하면 층을 넘어갈 수록 모양이 일그러지는데 이 때는 활성화 함수로 시그모이드가 아닌 쌍곡선 함수 <strong>tanh함수</strong>를 이용하면 개선된다. 시그모이드 함수와 같은 ’S’자 모양 곡선 함수이지만, tanh는 (0, 0), sigmoid는 (0, 0.5)에서 대칭인 곡선이며, 활성화 함수로는 원점에서 대칭인 함수가 바람직하다고 알려져있다.</p> <h3 id="relu의-초깃값">ReLU의 초깃값</h3> <p>Xavier 초깃값은 활성화 함수가 선형인 것을 전제로 이끈 결과이며, 시그모이드와 tanh 함수는 좌우 대칭이라 중앙 부근이 선형인 함수로 볼 수 있다. 그러나 ReLU는 ReLU에 특화된 초깃값을 이용할 것을 권장하며, 이 특화된 초깃값을 <strong>He 초깃값</strong>이라고 한다. He 초깃값은 앞 계층의 노드가 $n$개일 때, 표준편차가 $\sqrt \frac{2}{n}$인 정규 분포를 사용한다. Xavier 초깃값과 다른 이유는 ReLU는 음의 영역이 0이라서 더 넓게 분포시키기 위해 2배의 계수가 필요하다고 해석할 수 있다.</p> <h2 id="결론">결론</h2> <p>실제로 Xavier 초깃값 결과를 보면 층이 깊어지면서 치우침이 조금씩 커진다. 실제로 층이 깊어지면 활성화값들의 치우침도 커지고, 학습할 때 ‘기울기 소실’ 문제를 일으킨다. 그러나 He 초깃값은 모든 층에서 균일하게 분포되었고, 층이 깊어져도 분포가 균일하게 유지되기에 역전파 때도 적절한 값이 나올것으로 기대할 수 있다.</p> <table> <thead> <tr> <th style="text-align: center">He</th> <th style="text-align: center">Xavier</th> </tr> </thead> <tbody> <tr> <td style="text-align: center">ReLU</td> <td style="text-align: center">Sigmoid, tanh …</td> </tr> </tbody> </table>]]></content><author><name></name></author><category term="DL"/><category term="Neural_Network"/><summary type="html"><![CDATA[가중치의 초기값]]></summary></entry><entry><title type="html">Batch Normalization</title><link href="https://ottersem.github.io/blog/2024/Batch-Normalization/" rel="alternate" type="text/html" title="Batch Normalization"/><published>2024-10-07T20:29:00+00:00</published><updated>2024-10-07T20:29:00+00:00</updated><id>https://ottersem.github.io/blog/2024/Batch-Normalization</id><content type="html" xml:base="https://ottersem.github.io/blog/2024/Batch-Normalization/"><![CDATA[<h1 id="배치-정규화">배치 정규화</h1> <p>각 층이 활성화를 적당히 퍼뜨리도록 ‘강제’하는 것을 <strong>배치 정규화Batch Normalization</strong>라고 한다. 배치 정규화는 2015년에 제안되어 세상에 알려진지 얼마 되지 않았지만 많은 연구자와 기술자들이 즐겨 사용하는데, 배치 정규화가 주목받는 이유는 다음과 같다.</p> <ul> <li>학습 속도 개선</li> <li>초깃값에 의존하지 않는다</li> <li>오버피팅을 억제한다(드롭아웃 등의 필요성 감소).</li> </ul> <p>배치 정규화는 활성화값이 적당히 분포되도록 조정하는 것이기에 활성화 함수의 앞 또는 뒤에 배치 정규화 계층을 삽입하여 작동한다. 배치 정규화는 학습 시 미니배치를 단위로 정규화 하는데, 데이터 분포가 평균이 0, 분산이 1이 되도록 정규화한다.</p> \[\mu_B \leftarrow \frac{1}{m} \sum_{i=1}^{m}x_i \\ \sigma_B^2 \leftarrow \frac{1}{m} \sum_{i=1}^m(x_i - \mu_B)^2 \\ \hat{x_i} \leftarrow \frac{x_i - \mu_B}{\sqrt{\sigma^2_B+\epsilon}}\] <p>미니배치 $B = (x_1,x_2,…,x_m)$이라는 m개의 입력 데이터의 집합에 대해 평균 $\mu_B$와 분산 $\sigma_B^2$을 구한다. 그리고 입력 데이터를 평균이 0, 분산이 1이 되게 정규화하고, 이 과정에서 $\epsilon$은 아주 작은 값으로, 0으로 나누는 상황을 예방하는 역할이다 또 배치 정규화 계층마다 이 정규화된 데이터에 고유한 확대와 이동 변환을 수행한다.</p> \[y_i \leftarrow \gamma\hat{x_i} + \beta\] <p>$\gamma$가 확대를 $\beta$가 이동을 담당하며, 두 값은 처음에는 각각 1, 0부터 시작하고 학습하면서 적합한 값으로 조정해간다.</p>]]></content><author><name></name></author><category term="DL"/><category term="Neural_Network"/><summary type="html"><![CDATA[배치 정규화]]></summary></entry><entry><title type="html">Parameter Renewal</title><link href="https://ottersem.github.io/blog/2024/Parameter-Renewal/" rel="alternate" type="text/html" title="Parameter Renewal"/><published>2024-10-06T20:29:00+00:00</published><updated>2024-10-06T20:29:00+00:00</updated><id>https://ottersem.github.io/blog/2024/Parameter-Renewal</id><content type="html" xml:base="https://ottersem.github.io/blog/2024/Parameter-Renewal/"><![CDATA[<h1 id="매개변수-갱신">매개변수 갱신</h1> <ul> <li>최적화 : 손실 함수의 값을 가능한 한 낮추는 매개변수의 최적값을 찾는 문제</li> </ul> <h2 id="확률적-경사-하강법sgd">확률적 경사 하강법SGD</h2> <p>확률적 경사 하강법이란 매개변수의 기울기를 구해, 기울어진 방향으로 매개변수 값을 갱신하는 일을 몇 번이고 반복해서 최적의 값에 다가가는 것이다.</p> \[W \leftarrow W - \eta \frac{\partial L}{\partial W}\] <p>$W$는 갱신할 가중치 매개변수이며 $\frac{\partial L}{\partial W}$은 $W$에 대한 손실 함수의 기울기이다. $\eta$는 학습률을 의미하며, 하이퍼 파라미터로 작용한다. 위 식에서 알 수 있듯이 SGD는 기울어진 방향으로 일정 거리만 가겠다는 단순한 방법이다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">SGD</span><span class="p">:</span>
	<span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">lr</span> <span class="o">=</span> <span class="mf">0.01</span><span class="p">):</span>
		<span class="n">self</span><span class="p">.</span><span class="n">lr</span> <span class="o">=</span> <span class="n">lr</span>

	<span class="k">def</span> <span class="nf">update</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">params</span><span class="p">,</span> <span class="n">grads</span><span class="p">):</span>
		<span class="k">for</span> <span class="n">key</span> <span class="ow">in</span> <span class="n">params</span><span class="p">.</span><span class="nf">keys</span><span class="p">():</span>
			<span class="n">params</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">-=</span> <span class="n">self</span><span class="p">.</span><span class="n">lr</span> <span class="o">*</span> <span class="n">grads</span><span class="p">[</span><span class="n">key</span><span class="p">]</span>
</code></pre></div></div> <p>lr은 learning rate를 의미한다. 학습률을 인스턴스 변수로 유지하고, update 메서드는 반복해서 호출된다.</p> <h3 id="단점">단점</h3> <p>SGD의 단점은 비등방성 함수에서는 탐색 경로가 비 효율적이라는 것이다. 무식하게 기울어진 방향으로 진행하는 방향이 그 이유이고, 또한, 지그재그로 탐색하는 근본 원인은 기울어진 방향이 본래의 최솟값과 다른 방향을 가리켜서이기도 하다.</p> <h2 id="momentum">Momentum</h2> <p>모멘텀 기법의 수식은 다음과 같다.</p> \[V \leftarrow \alpha v - \eta \frac{\partial L}{\partial W} \\ W \leftarrow W + v\] <p>SGD에서는 볼 수 없었던 $v$라는 변수는 물리에서 말하는 속도에 해당한다. 모멘텀은 공이 그릇의 바닥을 구르는 듯한 움직임을 보여주는데, $\alpha v$항이 물체가 아무런 힘을 받지 않을 때 서서히 하강시키는 역할을 하기 때문이다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Momentum</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">lr</span><span class="o">=</span><span class="mf">0.01</span><span class="p">,</span> <span class="n">momentum</span><span class="o">=</span><span class="mf">0.9</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">lr</span> <span class="o">=</span> <span class="n">lr</span>
        <span class="n">self</span><span class="p">.</span><span class="n">momentum</span> <span class="o">=</span> <span class="n">momentum</span>
        <span class="n">self</span><span class="p">.</span><span class="n">v</span> <span class="o">=</span> <span class="bp">None</span>
        
    <span class="k">def</span> <span class="nf">update</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">params</span><span class="p">,</span> <span class="n">grads</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">v</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="n">v</span> <span class="o">=</span> <span class="p">{}</span>
            <span class="k">for</span> <span class="n">key</span><span class="p">,</span> <span class="n">val</span> <span class="ow">in</span> <span class="n">params</span><span class="p">.</span><span class="nf">items</span><span class="p">():</span>                                
                <span class="n">self</span><span class="p">.</span><span class="n">v</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros_like</span><span class="p">(</span><span class="n">val</span><span class="p">)</span>
                
        <span class="k">for</span> <span class="n">key</span> <span class="ow">in</span> <span class="n">params</span><span class="p">.</span><span class="nf">keys</span><span class="p">():</span>
            <span class="n">self</span><span class="p">.</span><span class="n">v</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">momentum</span><span class="o">*</span><span class="n">self</span><span class="p">.</span><span class="n">v</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">-</span> <span class="n">self</span><span class="p">.</span><span class="n">lr</span><span class="o">*</span><span class="n">grads</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> 
            <span class="n">params</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">+=</span> <span class="n">self</span><span class="p">.</span><span class="n">v</span><span class="p">[</span><span class="n">key</span><span class="p">]</span>
</code></pre></div></div> <p>인스턴스 변수 v는 속도의 역할을 한다. 초기화 때는 아무 값도 담지 않지만, update가 처음 호출될 때 매개변수와 같은 구조의 데이터를 딕셔너리 변수로 저장한다.<br/> 전체적인 갱신의 양상은 SGD와 유사하지만 보다 ‘지그재그 정도’가 덜하다. 이는 x 축의 힘은 아주 작지만 방향이 변하지 않아 한 방향으로 일정하게 가속하기 때문이다. 반대로 y 축의 힘은 크지만 위아래로 번갈아 받아 상충하여 y 축 방향의 속도는 안정적이지 않다.</p> <h2 id="adagrad">AdaGrad</h2> <p>신경망 학습에서는 학습률 값이 중요한데, 이 값이 <strong>너무 작으면</strong> 학습 시간이 너무 길어지고, 반대로 <strong>너무 크면</strong> 발산하여 학습이 제대로 이뤄지지 않는다.<br/> 이 학습률을 정하는 기술로 <strong>학습률 감소learning rate decay</strong>가 있다. 학습률을 서서히 낮추는 간단한 방법은 매개변수 ‘전체’의 학습률 값을 일괄적으로 낮추는 것이다. 이를 발전시켜 AdaGrad는 ‘각각의’ 매개변수에 ‘맞춤형’ 값을 만들어준다.</p> \[h \leftarrow h+ \frac{\eta L}{\eta W} \odot \frac{\eta L}{\eta W} \\ W \leftarrow W - \eta \frac{1}{\sqrt h}\frac{\eta L}{\eta W}\] <p>새로운 변수 h는 <strong>기존 기울기 값을 제곱하여 계속 더해준다.</strong> 그리고 매개변수를 갱신할 때 $\frac{1}{\sqrt h}$를 곱해 학습률을 조정한다. 매개변수의 원소 중 많이 움직인 원소는 학습률이 낮아진다는 뜻인데, 학습률 감소가 매개변수의 원소마다 다르게 적용됨을 뜻한다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">AdaGrad</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">lr</span><span class="o">=</span><span class="mf">0.01</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">lr</span> <span class="o">=</span> <span class="n">lr</span>
        <span class="n">self</span><span class="p">.</span><span class="n">h</span> <span class="o">=</span> <span class="bp">None</span>
        
    <span class="k">def</span> <span class="nf">update</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">params</span><span class="p">,</span> <span class="n">grads</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">h</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="n">h</span> <span class="o">=</span> <span class="p">{}</span>
            <span class="k">for</span> <span class="n">key</span><span class="p">,</span> <span class="n">val</span> <span class="ow">in</span> <span class="n">params</span><span class="p">.</span><span class="nf">items</span><span class="p">():</span>
                <span class="n">self</span><span class="p">.</span><span class="n">h</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros_like</span><span class="p">(</span><span class="n">val</span><span class="p">)</span>
            
        <span class="k">for</span> <span class="n">key</span> <span class="ow">in</span> <span class="n">params</span><span class="p">.</span><span class="nf">keys</span><span class="p">():</span>
            <span class="n">self</span><span class="p">.</span><span class="n">h</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">+=</span> <span class="n">grads</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">*</span> <span class="n">grads</span><span class="p">[</span><span class="n">key</span><span class="p">]</span>
            <span class="n">params</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">-=</span> <span class="n">self</span><span class="p">.</span><span class="n">lr</span> <span class="o">*</span> <span class="n">grads</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">/</span> <span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">h</span><span class="p">[</span><span class="n">key</span><span class="p">])</span> <span class="o">+</span> <span class="mf">1e-7</span><span class="p">)</span>
</code></pre></div></div> <p>주의해야 할 점은 1e-7이라는 작은 값을 더해 self.h[key]에 0 이 담겨있어도 0으로 나누는 상황을 막아준다.</p> <h2 id="adam">Adam</h2> <p>모멘텀과 AdaGrad를 융합한 기법이 Adam이다. 두 방법의 이점을 조합해 매개변수 공간을 효율적으로 탐색하는 기능을 기대할 수 있고, 하이퍼 파라미터의 ‘편향 보정’이 진행된다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Adam</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">lr</span><span class="o">=</span><span class="mf">0.001</span><span class="p">,</span> <span class="n">beta1</span><span class="o">=</span><span class="mf">0.9</span><span class="p">,</span> <span class="n">beta2</span><span class="o">=</span><span class="mf">0.999</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">lr</span> <span class="o">=</span> <span class="n">lr</span>
        <span class="n">self</span><span class="p">.</span><span class="n">beta1</span> <span class="o">=</span> <span class="n">beta1</span>
        <span class="n">self</span><span class="p">.</span><span class="n">beta2</span> <span class="o">=</span> <span class="n">beta2</span>
        <span class="n">self</span><span class="p">.</span><span class="nb">iter</span> <span class="o">=</span> <span class="mi">0</span>
        <span class="n">self</span><span class="p">.</span><span class="n">m</span> <span class="o">=</span> <span class="bp">None</span>
        <span class="n">self</span><span class="p">.</span><span class="n">v</span> <span class="o">=</span> <span class="bp">None</span>
        
    <span class="k">def</span> <span class="nf">update</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">params</span><span class="p">,</span> <span class="n">grads</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">m</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="n">m</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">v</span> <span class="o">=</span> <span class="p">{},</span> <span class="p">{}</span>
            <span class="k">for</span> <span class="n">key</span><span class="p">,</span> <span class="n">val</span> <span class="ow">in</span> <span class="n">params</span><span class="p">.</span><span class="nf">items</span><span class="p">():</span>
                <span class="n">self</span><span class="p">.</span><span class="n">m</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros_like</span><span class="p">(</span><span class="n">val</span><span class="p">)</span>
                <span class="n">self</span><span class="p">.</span><span class="n">v</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros_like</span><span class="p">(</span><span class="n">val</span><span class="p">)</span>
        
        <span class="n">self</span><span class="p">.</span><span class="nb">iter</span> <span class="o">+=</span> <span class="mi">1</span>
        <span class="n">lr_t</span>  <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">lr</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="mf">1.0</span> <span class="o">-</span> <span class="n">self</span><span class="p">.</span><span class="n">beta2</span><span class="o">**</span><span class="n">self</span><span class="p">.</span><span class="nb">iter</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="mf">1.0</span> <span class="o">-</span> <span class="n">self</span><span class="p">.</span><span class="n">beta1</span><span class="o">**</span><span class="n">self</span><span class="p">.</span><span class="nb">iter</span><span class="p">)</span>         
        
        <span class="k">for</span> <span class="n">key</span> <span class="ow">in</span> <span class="n">params</span><span class="p">.</span><span class="nf">keys</span><span class="p">():</span>
            <span class="c1">#self.m[key] = self.beta1*self.m[key] + (1-self.beta1)*grads[key]
</span>            <span class="c1">#self.v[key] = self.beta2*self.v[key] + (1-self.beta2)*(grads[key]**2)
</span>            <span class="n">self</span><span class="p">.</span><span class="n">m</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">+=</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">self</span><span class="p">.</span><span class="n">beta1</span><span class="p">)</span> <span class="o">*</span> <span class="p">(</span><span class="n">grads</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">-</span> <span class="n">self</span><span class="p">.</span><span class="n">m</span><span class="p">[</span><span class="n">key</span><span class="p">])</span>
            <span class="n">self</span><span class="p">.</span><span class="n">v</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">+=</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">self</span><span class="p">.</span><span class="n">beta2</span><span class="p">)</span> <span class="o">*</span> <span class="p">(</span><span class="n">grads</span><span class="p">[</span><span class="n">key</span><span class="p">]</span><span class="o">**</span><span class="mi">2</span> <span class="o">-</span> <span class="n">self</span><span class="p">.</span><span class="n">v</span><span class="p">[</span><span class="n">key</span><span class="p">])</span>
            
            <span class="n">params</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">-=</span> <span class="n">lr_t</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">m</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">/</span> <span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">v</span><span class="p">[</span><span class="n">key</span><span class="p">])</span> <span class="o">+</span> <span class="mf">1e-7</span><span class="p">)</span>
            
            <span class="c1">#unbias_m += (1 - self.beta1) * (grads[key] - self.m[key]) # correct bias
</span>            <span class="c1">#unbisa_b += (1 - self.beta2) * (grads[key]*grads[key] - self.v[key]) # correct bias
</span>            <span class="c1">#params[key] += self.lr * unbias_m / (np.sqrt(unbisa_b) + 1e-7)
</span></code></pre></div></div> <p>Adam은 하이퍼 파라미터를 3개 설정하는데 하나는 학습률$\eta \ or \ \alpha$, 일차 모멘텀용 계수$\beta _1$과 이차 모멘텀용 계수 $\beta _2$이다.</p>]]></content><author><name></name></author><category term="DL"/><category term="Neural_Network"/><summary type="html"><![CDATA[파라미터 갱신]]></summary></entry><entry><title type="html">Backpropagation</title><link href="https://ottersem.github.io/blog/2024/Backpropagation/" rel="alternate" type="text/html" title="Backpropagation"/><published>2024-09-28T21:39:00+00:00</published><updated>2024-09-28T21:39:00+00:00</updated><id>https://ottersem.github.io/blog/2024/Backpropagation</id><content type="html" xml:base="https://ottersem.github.io/blog/2024/Backpropagation/"><![CDATA[<h1 id="backpropagation">Backpropagation</h1> <p>오차역전파를 노드(원)과 에지(선)으로 이루어진 계산 그래프라고 생각해보자.<br/> 에지에는 숫자가, 노드에는 연산자가 할당된다. 이때 계산을 왼쪽에서 오른쪽으로 진행하는 단계(출발점에서 종착점으로의 전파)를 <strong>순전파</strong>, 그 반대로 오른쪽에서 왼쪽으로 계산하는 단계(종착점에서 출발점으로의 전파)를 <strong>역전파</strong>라고한다.</p> <h2 id="국소적-계산">국소적 계산</h2> <p>계산 그래프의 특진은 ‘국소적 계산’을 전파함으로써 최종 결과를 얻는다. 여기서 ‘국소적’이란 <strong>’자신과 직접 관계된 작은 범위’</strong>라는 뜻이다. 즉, 전체에서 어떤 일이 일어나든 자신(노드)과 관계된 정보만으로 결과를 출력할 수 있다는 것이다. 전체 계산이 아무리 복잡하더라도 각 단계에서 하는 일은 해당 노드의 ‘국소적 계산’일 뿐이다.</p> <h2 id="연쇄법칙chain-rule">연쇄법칙Chain Rule</h2> <p>역전파는 ‘국소적인 미분’을 순방향과는 반대인 오른쪽에서 왼쪽으로 전달한다. 또한, 이 ‘국소적 미분’을 전달하는 원리는 <strong>연쇄법칙</strong>에 따른 것이다. 계산그래프를 사용한 역전파의 예를 살펴보자. <img src="#" alt=""/> 그림과 같이 역전파의 계산 절차는 신호 $E$에 노드의 국소적 미분($\frac{\partial y}{\partial x}$)을 곱한 후 다음 노드로 전달하는 것이다. 여기에서 국소적 미분은 순전파 때의 $y=f(x)$ 계산의 미분을 구한다는 것이며, 이는 $x$의 $y$에 대한 미분을 구한다는 뜻이다. 그리고 이 국소적 미분을 상류에서 전달된 값에 곱해 앞 쪽 노드로 전달하는 것이다.</p> <h3 id="합성함수">합성함수</h3> <p><strong>합성 함수란 여러 함수로 구성된 함수</strong>이다. 연쇄법칙은 합성 함수의 미분에 대한 성질이며 다음과 같이 정의된다.</p> <blockquote> <p>합성 함수의 미분은 합성 함수를 구성하는 각 함수의 미분의 곱으로 나타낼 수 있다.</p> </blockquote> <p>다음 수식으로 예를 들어보자.</p> \[z=t^2 \\ t = x+y\] <p>이때 $\frac{\partial z}{\partial x} = \frac{\partial z}{\partial t}\frac{\partial t}{\partial x}$ 처럼 나타낼 수 있으며, $\frac{\partial z}{\partial t} =2t, \frac{\partial t}{\partial x} = 1$ 이므로 $\frac{\partial z}{\partial x} = \frac{\partial z}{\partial t}\frac{\partial t}{\partial x} =2t \times 1 = 2(x+y)$처럼 계산할 수 있다.<br/> 정리하자면 역전파의 계산 절차에서는 노드로 들어온 입력 신호에 그 노드의 국소적 미분(편미분)을 곱한 후 다음 노드로 전달한다.</p> <h2 id="역전파">역전파</h2> <h3 id="덧셈-노드의-역전파">덧셈 노드의 역전파</h3> <p>$z = x + y$일 때 $z$의 미분은 해석적으로 다음과 같다.</p> \[\frac{\partial z}{\partial x} = 1, \ \frac{\partial z}{\partial y} = 1\] <p>따라서 덧셈 노드의 역전파는 입력 신호를 다음 노드로 출력할 뿐이므로 상류로부터 노드에 전달된 값을 하류로 전달한다.</p> <h3 id="곱셈-노드의-역전파">곱셈 노드의 역전파</h3> <p>$z = xy$일 때 $z$의 미분은 해석적으로 다음과 같다. \(\frac{\partial z}{\partial x} = y, \ \frac{\partial z}{\partial y} = x\)</p> <p>따라서 곱셈 노드의 역전파는 상류의 값에 순전파 때의 입력 신호를 ‘서로 바꾼 값’을 곱해서 하류로 보낸다. 다만 덧셈의 역전파에서는 상류의 값을 그대로 흘려보내서 순방향 입력 신호의 값이 필요하지 않지만, 곱셈의 역전파는 순방향 입력 신호의 값이 필요하기에 곱셈 노드를 구현할 때는 순전파의 입력 신호를 변수에 저장해둔다.</p> <h2 id="활성화-함수-계층-구현">활성화 함수 계층 구현</h2> <h3 id="relu">ReLU</h3> <p>ReLU의 수식과 그 미분은 다음과 같다.</p> \[y = \begin{cases} x(x&gt;0)\\ 0(x\le0) \end{cases} \qquad \frac{\partial y}{\partial x} = \begin{cases} 1(x&gt;0)\\ 0(x\le0) \end{cases}\] <p>순전파 때의 입력인 $x$가 0보다 크면 역전파는 상류의 값을 그대로 하류로 전달하는 반면, 순전파 때 $x$가 0 이하면 역전파 때는 하류로 신호를 전달하지 않는다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Relu</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">mask</span> <span class="o">=</span> <span class="bp">None</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">mask</span> <span class="o">=</span> <span class="p">(</span><span class="n">x</span> <span class="o">&lt;=</span> <span class="mi">0</span><span class="p">)</span>
        <span class="n">out</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="nf">copy</span><span class="p">()</span>
        <span class="n">out</span><span class="p">[</span><span class="n">self</span><span class="p">.</span><span class="n">mask</span><span class="p">]</span> <span class="o">=</span> <span class="mi">0</span>

        <span class="k">return</span> <span class="n">out</span>

    <span class="k">def</span> <span class="nf">backward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">dout</span><span class="p">):</span>
        <span class="n">dout</span><span class="p">[</span><span class="n">self</span><span class="p">.</span><span class="n">mask</span><span class="p">]</span> <span class="o">=</span> <span class="mi">0</span>
        <span class="n">dx</span> <span class="o">=</span> <span class="n">dout</span>

        <span class="k">return</span> <span class="n">dx</span>
</code></pre></div></div> <p>위와 같이 ReLU 클래스를 구현하면 순전파 때 만들어둔 mask를 사용하여 True인 곳에서는 상류에서 전파된 dout을 0으로 설정한다.</p> <h3 id="sigmoid">Sigmoid</h3> <p>시그모이드 함수는 다음과 같다.</p> \[y=\frac{1}{1+exp(x)}\] <p>곱셈 노드와 같이 나눗셈 노드를 미분하면 다음과 같다.</p> \[\begin{align} \frac{\partial y}{\partial x} = -\frac{1}{x^2} \\ =-y^2 \end{align}\] <p>위 식에 따르면 나눗셈 노드의 역전파 때는 상류에서 흘러온 값에 $-y^2$(순전파의 출력을 제곱한 후 마이너스를 붙인 값)을 곱해 하류로 전달한다.<br/> exp노드는 지수연산을 수행하며 지수함수의 미분은 형태를 유지하기에 $\frac{\partial y}{\partial x} = exp(x)$와 같다. 따라서 sigmoid함수의 역전파의 최종 출력은 $\frac{\partial L}{\partial y}y^2exp(-x)$값이 하류로 전달된다.<br/> 최종 출력 수식에서 알 수 있듯 순전파의 입력$x$와 출력 $y$만으로 계산할 수 있는데, 다시 한 번 정리하면 다음과 같이 $y$(순전파의 출력)만으로도 계산할 수 있다.</p> \[\begin{align} \frac{\partial L}{\partial y}y^2exp(-x) = \frac{\partial L}{\partial y}\frac{1}{(1+exp(-x))^2}exp(-x) \\ =\frac{\partial L}{\partial y}\frac{1}{1+exp(-x)}\frac{exp(-x)}{1+exp(-x)} \\ =\frac{\partial L}{\partial y}y(1-y) \end{align}\] <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Sigmoid</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">out</span> <span class="o">=</span> <span class="bp">None</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="n">out</span> <span class="o">=</span> <span class="nf">sigmoid</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">out</span> <span class="o">=</span> <span class="n">out</span>
        <span class="k">return</span> <span class="n">out</span>

    <span class="k">def</span> <span class="nf">backward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">dout</span><span class="p">):</span>
        <span class="n">dx</span> <span class="o">=</span> <span class="n">dout</span> <span class="o">*</span> <span class="p">(</span><span class="mf">1.0</span> <span class="o">-</span> <span class="n">self</span><span class="p">.</span><span class="n">out</span><span class="p">)</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">out</span>

        <span class="k">return</span> <span class="n">dx</span>
</code></pre></div></div> <p>위와 같이 sigmoid를 구현하면 순전파의 출력을 인스턴스 변수 out에 보관했다가 역전파 때에 그 값을 재사용한다.</p> <h2 id="affinesoftmax-계층">Affine/Softmax 계층</h2> <h3 id="affine계층">Affine계층</h3> <p>신경망의 순전파 시에 수행하는 행렬의 곱을 <strong>어파인 변환 Affine Transformation</strong>라고 한다. 따라서 어파인 변환을 수행하는 처리를 ‘Affine 계층’이라는 이름으로 구현한다.<br/> 행렬의 곱 계산은 대응하는 차원의 원소 수를 일치시키는 게 핵심이다. 예를 들자면 $X \cdot W = O$ 에서 차례로 (2,), (2,3), (3,) 처럼 대응하는 차원의 원소 수를 일치시켜야 한다.<br/> 위 내용을 참고하면 $Y=np.dot(X,W)+b$을 계산하고 $Y$를 활성화 함수로 변환해 다음 층으로 전파하는 것이 신경망 순전파의 흐름임을 알 수 있다.<br/> 행렬을 사용한 역전파도 행렬의 원소마다 전개해보면 스칼라값을 사용한 계산과 같은 순서로 생각할 수 있다. 실제로 X와 W의 역전파를 각각 전개해보면 다음과 같다. \(\frac{\partial L}{\partial X} _{(2,)}= \frac{\partial L}{\partial Y}_{(3,)} \cdot W^T_{(3,2)},\ \frac{\partial L}{\partial W}_{(2,3)} = X^T_{(2,1)} \cdot \frac{\partial L}{\partial Y}_{(1,3)}\) 눈 여겨봐야 할 점은 $X,W$는 각각의 역전파$\frac{\partial L}{\partial X}, \frac{\partial L}{\partial W}$와 같은 형상이라는 점이다. 행렬의 곱에서는 대응하는 차원의 원소 수를 일치시켜야 하는데, 이를 위해서는 전치행렬을 동원해야 할 수도 있기 때문이다. 정리하자면 행렬 곱의 역전파는 행렬의 대응하는 차원의 원소 수가 일치하도록 곱을 조립하여 구할 수 있다.</p> <h4 id="배치용-affine계층">배치용 Affine계층</h4> <p>상술한 Affine계층이 입력 데이터로 X 하나만을 고려한 것이었다면, 배치용 Affine계층은 X의 형상이 (N,2)가 된 것이다. 계산 과정 또한 역전파 시의 행렬의 형상에 주의하면 이전과 같이 $\frac{\partial L}{\partial X}, \frac{\partial L}{\partial W}$를 도출할 수 있다.</p> \[\frac{\partial L}{\partial X} _{(N,2)}= \frac{\partial L}{\partial Y}_{(N,3)} \cdot W^T_{(3,2)},\ \frac{\partial L}{\partial W}_{(2,3)} = X^T_{(2,N)} \cdot \frac{\partial L}{\partial Y}_{(N,3)}\] <p>\(*\ \frac{\partial L}{\partial B}_{(3)} = \frac{\partial L}{\partial Y}_{(N,3)}의 \ 첫 \ 번째 \ 축(열방향)의 \ 합\) 순전파의 편향 덧셈은 각각의 데이터에 더해진다. 따라서 역전파 때는 각 데이터의 역전파 값이 편향의 원소에 모여야 하기에 편향의 역전파는 그 두 데이터에 대한 미분을 데이터마다 더해서 구한다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;&gt;&gt;</span> <span class="n">dY</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([[</span><span class="mi">1</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">3</span><span class="p">],[</span><span class="mi">4</span><span class="p">,</span><span class="mi">5</span><span class="p">,</span><span class="mi">6</span><span class="p">]])</span>
<span class="o">&gt;&gt;&gt;</span> <span class="n">dY</span>
<span class="nf">array</span><span class="p">([[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">],</span>
       <span class="p">[</span><span class="mi">4</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">6</span><span class="p">]])</span>
<span class="o">&gt;&gt;&gt;</span> <span class="n">dB</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">dY</span><span class="p">,</span> <span class="n">axis</span> <span class="o">=</span> <span class="mi">0</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="n">dB</span>
<span class="nf">array</span><span class="p">([</span><span class="mi">5</span><span class="p">,</span> <span class="mi">7</span><span class="p">,</span> <span class="mi">9</span><span class="p">])</span>
</code></pre></div></div> <h3 id="softmax-with-loss계층">Softmax-with-loss계층</h3> <p>소프트맥스 함수는 입력 값을 정규화하여 출력한다. 다시말하자면 소프트맥스 함수는 정답을 확률로 해석할 수 있는 틀을 제공해주며, 가장 높은 점수만을 알면 되는 <strong>추론</strong>과정에서는 사용되지 않고, <strong>학습</strong>과정에서 사용된다. 교차 엔트로피 오차를 포함한 소프트맥스 계층을 구현하기에 softmax-with-loss 계층이라는 이름으로 구현한다. 3클래스 분류를 가정하고 예를 들어보자.<br/> 우선 이전 계층에서 3개의 점수$(a_1,a_2,a_3)$를 입력받고 정규화 하여 $(y_1,y_2,y_3)$를 출력한다. 교차 엔트로피 오차 계층에서는 소프트맥스 계층의 출력$(y_1,y_2,y_3)$과 정답 레이블$(t_1,t_2,t_3)$를 입력받고, 이 데이터를 통해 손실$L$을 출력하는 것이 순전파 과정이다.<br/> 이제 역전파 과정을 살펴보자. 소프트맥스 계층의 역전파는 $(y_1 - t_1, y_2 - t_2, y_3 - t_3)$라는 깔끔한 결과를 내놓는데 이 값은 소프트맥스 계층의 출력과 정답 레이블의 차분이다. <strong>신경망의 역전파에서는 이 차이인 오차가 앞 계층으로 전달</strong>된다.<br/> 다시한번 되새겨 보는 신경망 학습의 목적은 <strong>신경망의 출력이 정답 레이블과 가까워 지도록 가중치 매개변수의 값을 조정하는 것</strong>이다. 따라서 소프트맥스 계층의 역전파 값은 <strong>신경망의 현재 출력과 정답 레이블의 오차를 있는 그대로 드러내는 것</strong>이다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">SoftmaxWithLoss</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">loss</span> <span class="o">=</span> <span class="bp">None</span> <span class="c1"># 손실함수
</span>        <span class="n">self</span><span class="p">.</span><span class="n">y</span> <span class="o">=</span> <span class="bp">None</span>    <span class="c1"># softmax의 출력
</span>        <span class="n">self</span><span class="p">.</span><span class="n">t</span> <span class="o">=</span> <span class="bp">None</span>    <span class="c1"># 정답 레이블(원-핫 인코딩 형태)
</span>        
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">t</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">t</span> <span class="o">=</span> <span class="n">t</span>
        <span class="n">self</span><span class="p">.</span><span class="n">y</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">loss</span> <span class="o">=</span> <span class="nf">cross_entropy_error</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">y</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">t</span><span class="p">)</span>
        
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">loss</span>

    <span class="k">def</span> <span class="nf">backward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">dout</span><span class="o">=</span><span class="mi">1</span><span class="p">):</span>
        <span class="n">batch_size</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">t</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
        <span class="n">dx</span> <span class="o">=</span> <span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">y</span> <span class="o">-</span> <span class="n">self</span><span class="p">.</span><span class="n">t</span><span class="p">)</span> <span class="o">/</span> <span class="n">batch_size</span> 
        <span class="k">return</span> <span class="n">dx</span>	
</code></pre></div></div> <h2 id="오차역전파법-구현">오차역전파법 구현</h2> <p>오차역전파법을 구성하는 TwoLayerNet클래스의 인스턴스 변수와 메서드는 다음과 같다.</p> <table> <thead> <tr> <th>인스턴스 변수</th> <th>설명</th> </tr> </thead> <tbody> <tr> <td>params</td> <td>딕셔너리 변수, 신경망의 매개변수 보관 [‘W1’]은 첫번째 층의 가중치, [‘b1’]은 첫번째 층의 편향</td> </tr> <tr> <td>layers</td> <td>순서가 있는 딕셔너리 변수, 신경망의 계층을 순서대로 유지하고 보관</td> </tr> <tr> <td>lastLayer</td> <td>신경망의 마지막 계층(SoftmaxWithLoss Layer)</td> </tr> </tbody> </table> <table> <thead> <tr> <th>메서드</th> <th>설명</th> </tr> </thead> <tbody> <tr> <td>__init__</td> <td>초기화 수행 인수는 앞에서부터 입력층 뉴런 수, 은닉층 뉴런 수, 출력층 뉴런 수, 가중치 초기화 시 정규분포의 스케일</td> </tr> <tr> <td>predict(self, x)</td> <td>예측을 수행 x는 이미지 데이터</td> </tr> <tr> <td>loss(self, x,t )</td> <td>손실 함수의 값을 구한다 x는 이미지 데이터, t는 정답 레이블</td> </tr> <tr> <td>accuracy(self, x, t)</td> <td>정확도를 구한다</td> </tr> <tr> <td>numerical_gradiant(self, x, t)</td> <td>가중치 매개변수의 기울기를 수치 미분 방식으로 구한다</td> </tr> <tr> <td>gradient(self,x,t)</td> <td>가중치 매개변수의 기울기를 오차 역전파법으로 구한다.</td> </tr> </tbody> </table> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">sys</span><span class="p">,</span> <span class="n">os</span>
<span class="n">sys</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">os</span><span class="p">.</span><span class="n">pardir</span><span class="p">)</span>  <span class="c1"># 부모 디렉터리의 파일을 가져올 수 있도록 설정
</span><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">from</span> <span class="n">common.layers</span> <span class="kn">import</span> <span class="o">*</span>
<span class="kn">from</span> <span class="n">common.gradient</span> <span class="kn">import</span> <span class="n">numerical_gradient</span>
<span class="kn">from</span> <span class="n">collections</span> <span class="kn">import</span> <span class="n">OrderedDict</span>


<span class="k">class</span> <span class="nc">TwoLayerNet</span><span class="p">:</span>

    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">input_size</span><span class="p">,</span> <span class="n">hidden_size</span><span class="p">,</span> <span class="n">output_size</span><span class="p">,</span> <span class="n">weight_init_std</span> <span class="o">=</span> <span class="mf">0.01</span><span class="p">):</span>
        <span class="c1"># 가중치 초기화
</span>        <span class="n">self</span><span class="p">.</span><span class="n">params</span> <span class="o">=</span> <span class="p">{}</span>
        <span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">[</span><span class="sh">'</span><span class="s">W1</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">weight_init_std</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">input_size</span><span class="p">,</span> <span class="n">hidden_size</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">[</span><span class="sh">'</span><span class="s">b1</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">hidden_size</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">[</span><span class="sh">'</span><span class="s">W2</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">weight_init_std</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">hidden_size</span><span class="p">,</span> <span class="n">output_size</span><span class="p">)</span> 
        <span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">[</span><span class="sh">'</span><span class="s">b2</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">output_size</span><span class="p">)</span>

        <span class="c1"># 계층 생성
</span>        <span class="n">self</span><span class="p">.</span><span class="n">layers</span> <span class="o">=</span> <span class="nc">OrderedDict</span><span class="p">()</span> <span class="c1">#신경망의 계층을 순서가 있는 딕셔너리 OrderedDict에 보관한다. 따라서 순전파 때는 추가한 순서대로 각 계층의 forward() 메서드를 호출하기만 하면 처리가 완료된다. 마찬가지로 역전파 때는 반대 순서로 호출하기만 하면 된다.
</span>        <span class="n">self</span><span class="p">.</span><span class="n">layers</span><span class="p">[</span><span class="sh">'</span><span class="s">Affine1</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="nc">Affine</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">[</span><span class="sh">'</span><span class="s">W1</span><span class="sh">'</span><span class="p">],</span> <span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">[</span><span class="sh">'</span><span class="s">b1</span><span class="sh">'</span><span class="p">])</span>
        <span class="n">self</span><span class="p">.</span><span class="n">layers</span><span class="p">[</span><span class="sh">'</span><span class="s">Relu1</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="nc">Relu</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">layers</span><span class="p">[</span><span class="sh">'</span><span class="s">Affine2</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="nc">Affine</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">[</span><span class="sh">'</span><span class="s">W2</span><span class="sh">'</span><span class="p">],</span> <span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">[</span><span class="sh">'</span><span class="s">b2</span><span class="sh">'</span><span class="p">])</span>

        <span class="n">self</span><span class="p">.</span><span class="n">lastLayer</span> <span class="o">=</span> <span class="nc">SoftmaxWithLoss</span><span class="p">()</span>
        
    <span class="k">def</span> <span class="nf">predict</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="k">for</span> <span class="n">layer</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">layers</span><span class="p">.</span><span class="nf">values</span><span class="p">():</span>
            <span class="n">x</span> <span class="o">=</span> <span class="n">layer</span><span class="p">.</span><span class="nf">forward</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        
        <span class="k">return</span> <span class="n">x</span>
        
    <span class="c1"># x : 입력 데이터, t : 정답 레이블
</span>    <span class="k">def</span> <span class="nf">loss</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">t</span><span class="p">):</span>
        <span class="n">y</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">lastLayer</span><span class="p">.</span><span class="nf">forward</span><span class="p">(</span><span class="n">y</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>
    
    <span class="k">def</span> <span class="nf">accuracy</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">t</span><span class="p">):</span>
        <span class="n">y</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="n">y</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">y</span><span class="p">,</span> <span class="n">axis</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">t</span><span class="p">.</span><span class="n">ndim</span> <span class="o">!=</span> <span class="mi">1</span> <span class="p">:</span> <span class="n">t</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">t</span><span class="p">,</span> <span class="n">axis</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        
        <span class="n">accuracy</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">y</span> <span class="o">==</span> <span class="n">t</span><span class="p">)</span> <span class="o">/</span> <span class="nf">float</span><span class="p">(</span><span class="n">x</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">])</span>
        <span class="k">return</span> <span class="n">accuracy</span>
        
    <span class="c1"># x : 입력 데이터, t : 정답 레이블
</span>    <span class="k">def</span> <span class="nf">numerical_gradient</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">t</span><span class="p">):</span>
        <span class="n">loss_W</span> <span class="o">=</span> <span class="k">lambda</span> <span class="n">W</span><span class="p">:</span> <span class="n">self</span><span class="p">.</span><span class="nf">loss</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>
        
        <span class="n">grads</span> <span class="o">=</span> <span class="p">{}</span>
        <span class="n">grads</span><span class="p">[</span><span class="sh">'</span><span class="s">W1</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="nf">numerical_gradient</span><span class="p">(</span><span class="n">loss_W</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">[</span><span class="sh">'</span><span class="s">W1</span><span class="sh">'</span><span class="p">])</span>
        <span class="n">grads</span><span class="p">[</span><span class="sh">'</span><span class="s">b1</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="nf">numerical_gradient</span><span class="p">(</span><span class="n">loss_W</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">[</span><span class="sh">'</span><span class="s">b1</span><span class="sh">'</span><span class="p">])</span>
        <span class="n">grads</span><span class="p">[</span><span class="sh">'</span><span class="s">W2</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="nf">numerical_gradient</span><span class="p">(</span><span class="n">loss_W</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">[</span><span class="sh">'</span><span class="s">W2</span><span class="sh">'</span><span class="p">])</span>
        <span class="n">grads</span><span class="p">[</span><span class="sh">'</span><span class="s">b2</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="nf">numerical_gradient</span><span class="p">(</span><span class="n">loss_W</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">params</span><span class="p">[</span><span class="sh">'</span><span class="s">b2</span><span class="sh">'</span><span class="p">])</span>
        
        <span class="k">return</span> <span class="n">grads</span>
        
    <span class="k">def</span> <span class="nf">gradient</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">t</span><span class="p">):</span>
        <span class="c1"># forward
</span>        <span class="n">self</span><span class="p">.</span><span class="nf">loss</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>

        <span class="c1"># backward
</span>        <span class="n">dout</span> <span class="o">=</span> <span class="mi">1</span>
        <span class="n">dout</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">lastLayer</span><span class="p">.</span><span class="nf">backward</span><span class="p">(</span><span class="n">dout</span><span class="p">)</span>
        
        <span class="n">layers</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">layers</span><span class="p">.</span><span class="nf">values</span><span class="p">())</span>
        <span class="n">layers</span><span class="p">.</span><span class="nf">reverse</span><span class="p">()</span>
        <span class="k">for</span> <span class="n">layer</span> <span class="ow">in</span> <span class="n">layers</span><span class="p">:</span>
            <span class="n">dout</span> <span class="o">=</span> <span class="n">layer</span><span class="p">.</span><span class="nf">backward</span><span class="p">(</span><span class="n">dout</span><span class="p">)</span>

        <span class="c1"># 결과 저장
</span>        <span class="n">grads</span> <span class="o">=</span> <span class="p">{}</span>
        <span class="n">grads</span><span class="p">[</span><span class="sh">'</span><span class="s">W1</span><span class="sh">'</span><span class="p">],</span> <span class="n">grads</span><span class="p">[</span><span class="sh">'</span><span class="s">b1</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">layers</span><span class="p">[</span><span class="sh">'</span><span class="s">Affine1</span><span class="sh">'</span><span class="p">].</span><span class="n">dW</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">layers</span><span class="p">[</span><span class="sh">'</span><span class="s">Affine1</span><span class="sh">'</span><span class="p">].</span><span class="n">db</span>
        <span class="n">grads</span><span class="p">[</span><span class="sh">'</span><span class="s">W2</span><span class="sh">'</span><span class="p">],</span> <span class="n">grads</span><span class="p">[</span><span class="sh">'</span><span class="s">b2</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">layers</span><span class="p">[</span><span class="sh">'</span><span class="s">Affine2</span><span class="sh">'</span><span class="p">].</span><span class="n">dW</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">layers</span><span class="p">[</span><span class="sh">'</span><span class="s">Affine2</span><span class="sh">'</span><span class="p">].</span><span class="n">db</span>

        <span class="k">return</span> <span class="n">grads</span>
</code></pre></div></div> <h3 id="오차역전파법으로-구한-기울기-검증">오차역전파법으로 구한 기울기 검증</h3> <p>기울기를 구하는 두 가지 방법은 수치 미분을 사용하는 방법과 해석적으로 수식을 풀어 구하는 방법이다. 수치 미분은 느리며, 오차역전파법을 제대로 구현해두면 수치 미분은 더 이상 필요하지 않다. 그렇다면 수치 미분의 쓸모는 어디에 있을까? 수치 미분은 <strong>오차역전파법을 정확하게 구현했는지 확인하기 위해 필요하다.</strong><br/> 이처럼 두 방식으로 구한 기울기가 거의 같음을 확인하는 작업을 <strong>기울기 확인</strong>이라고 한다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">sys</span><span class="p">,</span> <span class="n">os</span>
<span class="n">sys</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">os</span><span class="p">.</span><span class="n">pardir</span><span class="p">)</span>  <span class="c1"># 부모 디렉터리의 파일을 가져올 수 있도록 설정
</span><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">from</span> <span class="n">dataset.mnist</span> <span class="kn">import</span> <span class="n">load_mnist</span>
<span class="kn">from</span> <span class="n">two_layer_net</span> <span class="kn">import</span> <span class="n">TwoLayerNet</span>

<span class="c1"># 데이터 읽기
</span><span class="p">(</span><span class="n">x_train</span><span class="p">,</span> <span class="n">t_train</span><span class="p">),</span> <span class="p">(</span><span class="n">x_test</span><span class="p">,</span> <span class="n">t_test</span><span class="p">)</span> <span class="o">=</span> <span class="nf">load_mnist</span><span class="p">(</span><span class="n">normalize</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">one_hot_label</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>

<span class="n">network</span> <span class="o">=</span> <span class="nc">TwoLayerNet</span><span class="p">(</span><span class="n">input_size</span><span class="o">=</span><span class="mi">784</span><span class="p">,</span> <span class="n">hidden_size</span><span class="o">=</span><span class="mi">50</span><span class="p">,</span> <span class="n">output_size</span><span class="o">=</span><span class="mi">10</span><span class="p">)</span>

<span class="n">x_batch</span> <span class="o">=</span> <span class="n">x_train</span><span class="p">[:</span><span class="mi">3</span><span class="p">]</span>
<span class="n">t_batch</span> <span class="o">=</span> <span class="n">t_train</span><span class="p">[:</span><span class="mi">3</span><span class="p">]</span>

<span class="n">grad_numerical</span> <span class="o">=</span> <span class="n">network</span><span class="p">.</span><span class="nf">numerical_gradient</span><span class="p">(</span><span class="n">x_batch</span><span class="p">,</span> <span class="n">t_batch</span><span class="p">)</span>
<span class="n">grad_backprop</span> <span class="o">=</span> <span class="n">network</span><span class="p">.</span><span class="nf">gradient</span><span class="p">(</span><span class="n">x_batch</span><span class="p">,</span> <span class="n">t_batch</span><span class="p">)</span>

<span class="c1"># 각 가중치의 절대 오차의 평균을 구한다.
</span><span class="k">for</span> <span class="n">key</span> <span class="ow">in</span> <span class="n">grad_numerical</span><span class="p">.</span><span class="nf">keys</span><span class="p">():</span>
    <span class="n">diff</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">average</span><span class="p">(</span> <span class="n">np</span><span class="p">.</span><span class="nf">abs</span><span class="p">(</span><span class="n">grad_backprop</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">-</span> <span class="n">grad_numerical</span><span class="p">[</span><span class="n">key</span><span class="p">])</span> <span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="n">key</span> <span class="o">+</span> <span class="sh">"</span><span class="s">:</span><span class="sh">"</span> <span class="o">+</span> <span class="nf">str</span><span class="p">(</span><span class="n">diff</span><span class="p">))</span>
</code></pre></div></div> <h3 id="오차역전파법을-사용한-학습-구현">오차역전파법을 사용한 학습 구현</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">sys</span><span class="p">,</span> <span class="n">os</span>
<span class="n">sys</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">os</span><span class="p">.</span><span class="n">pardir</span><span class="p">)</span>

<span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">from</span> <span class="n">dataset.mnist</span> <span class="kn">import</span> <span class="n">load_mnist</span>
<span class="kn">from</span> <span class="n">two_layer_net</span> <span class="kn">import</span> <span class="n">TwoLayerNet</span>

<span class="c1"># 데이터 읽기
</span><span class="p">(</span><span class="n">x_train</span><span class="p">,</span> <span class="n">t_train</span><span class="p">),</span> <span class="p">(</span><span class="n">x_test</span><span class="p">,</span> <span class="n">t_test</span><span class="p">)</span> <span class="o">=</span> <span class="nf">load_mnist</span><span class="p">(</span><span class="n">normalize</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">one_hot_label</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>

<span class="n">network</span> <span class="o">=</span> <span class="nc">TwoLayerNet</span><span class="p">(</span><span class="n">input_size</span><span class="o">=</span><span class="mi">784</span><span class="p">,</span> <span class="n">hidden_size</span><span class="o">=</span><span class="mi">50</span><span class="p">,</span> <span class="n">output_size</span><span class="o">=</span><span class="mi">10</span><span class="p">)</span>

<span class="n">iters_num</span> <span class="o">=</span> <span class="mi">10000</span>
<span class="n">train_size</span> <span class="o">=</span> <span class="n">x_train</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
<span class="n">batch_size</span> <span class="o">=</span> <span class="mi">100</span>
<span class="n">learning_rate</span> <span class="o">=</span> <span class="mf">0.1</span>

<span class="n">train_loss_list</span> <span class="o">=</span> <span class="p">[]</span>
<span class="n">train_acc_list</span> <span class="o">=</span> <span class="p">[]</span>
<span class="n">test_acc_list</span> <span class="o">=</span> <span class="p">[]</span>

<span class="n">iter_per_epoch</span> <span class="o">=</span> <span class="nf">max</span><span class="p">(</span><span class="n">train_size</span> <span class="o">/</span> <span class="n">batch_size</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>

<span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">iters_num</span><span class="p">):</span>
    <span class="n">batch_mask</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">choice</span><span class="p">(</span><span class="n">train_size</span><span class="p">,</span> <span class="n">batch_size</span><span class="p">)</span>
    <span class="n">x_batch</span> <span class="o">=</span> <span class="n">x_train</span><span class="p">[</span><span class="n">batch_mask</span><span class="p">]</span>
    <span class="n">t_batch</span> <span class="o">=</span> <span class="n">t_train</span><span class="p">[</span><span class="n">batch_mask</span><span class="p">]</span>
    
    <span class="c1"># 기울기 계산
</span>    <span class="c1">#grad = network.numerical_gradient(x_batch, t_batch) # 수치 미분 방식
</span>    <span class="n">grad</span> <span class="o">=</span> <span class="n">network</span><span class="p">.</span><span class="nf">gradient</span><span class="p">(</span><span class="n">x_batch</span><span class="p">,</span> <span class="n">t_batch</span><span class="p">)</span> <span class="c1"># 오차역전파법 방식(훨씬 빠르다)
</span>    
    <span class="c1"># 갱신
</span>    <span class="k">for</span> <span class="n">key</span> <span class="ow">in</span> <span class="p">(</span><span class="sh">'</span><span class="s">W1</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">b1</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">W2</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">b2</span><span class="sh">'</span><span class="p">):</span>
        <span class="n">network</span><span class="p">.</span><span class="n">params</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">-=</span> <span class="n">learning_rate</span> <span class="o">*</span> <span class="n">grad</span><span class="p">[</span><span class="n">key</span><span class="p">]</span>
    
    <span class="n">loss</span> <span class="o">=</span> <span class="n">network</span><span class="p">.</span><span class="nf">loss</span><span class="p">(</span><span class="n">x_batch</span><span class="p">,</span> <span class="n">t_batch</span><span class="p">)</span>
    <span class="n">train_loss_list</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">loss</span><span class="p">)</span>
    
    <span class="k">if</span> <span class="n">i</span> <span class="o">%</span> <span class="n">iter_per_epoch</span> <span class="o">==</span> <span class="mi">0</span><span class="p">:</span>
        <span class="n">train_acc</span> <span class="o">=</span> <span class="n">network</span><span class="p">.</span><span class="nf">accuracy</span><span class="p">(</span><span class="n">x_train</span><span class="p">,</span> <span class="n">t_train</span><span class="p">)</span>
        <span class="n">test_acc</span> <span class="o">=</span> <span class="n">network</span><span class="p">.</span><span class="nf">accuracy</span><span class="p">(</span><span class="n">x_test</span><span class="p">,</span> <span class="n">t_test</span><span class="p">)</span>
        <span class="n">train_acc_list</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">train_acc</span><span class="p">)</span>
        <span class="n">test_acc_list</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">test_acc</span><span class="p">)</span>
        <span class="nf">print</span><span class="p">(</span><span class="n">train_acc</span><span class="p">,</span> <span class="n">test_acc</span><span class="p">)</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="DL"/><category term="Neural_Network"/><summary type="html"><![CDATA[오차역전파법]]></summary></entry><entry><title type="html">Class</title><link href="https://ottersem.github.io/blog/2024/Class/" rel="alternate" type="text/html" title="Class"/><published>2024-09-13T21:50:00+00:00</published><updated>2024-09-13T21:50:00+00:00</updated><id>https://ottersem.github.io/blog/2024/Class</id><content type="html" xml:base="https://ottersem.github.io/blog/2024/Class/"><![CDATA[<h1 id="클래스class">클래스Class</h1> <p>클래스와 클래스 사이가 어떤 관계를 형성하는 것은 흔히 볼 수 있으며, 그 방법은 IS-A와 HAS-A가 있다.</p> <h2 id="is-a-상속inheritance">IS-A: 상속Inheritance</h2> <p>IS-A는 ‘~는 ~의 한 종류다’를 의미한다. 예를 들어 ‘노트북은 컴퓨터의 한 종류다’는 문장은 관계가 성립하므로 컴퓨터와 노트북은 IS-A관계다. 위 문장을 영어로 표현하면 ‘A laptop <strong>IS-A</strong> computer’이므로 IS-A관계임을 확인할 수 있다.<br/> IS-A를 표현할 때는 상속(Inheritance)를 사용한다. 상속은 객체 지향의 근간이라고 불릴 만큼 중요하며 유용하지만, IS-A 관계가 아닐 때 상속을 하면 클래스 설계에 어려움이 따른다.<br/> 상속 관계에서 상속을 하는 클래스를 [기본클래스, 부모클래스, 슈퍼클래스]라고 하며, 상속 받는 클래스를 [파생클래스, 자식클래스, 서브클래스]라고 한다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Computer</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">cpu</span><span class="p">,</span> <span class="n">ram</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">CPU</span> <span class="o">=</span> <span class="n">cpu</span>
        <span class="n">self</span><span class="p">.</span><span class="n">RAM</span> <span class="o">=</span> <span class="n">ram</span>
        
    <span class="k">def</span> <span class="nf">browse</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">browse</span><span class="sh">'</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">work</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">work</span><span class="sh">'</span><span class="p">)</span>

<span class="k">class</span> <span class="nc">Laptop</span><span class="p">(</span><span class="n">Computer</span><span class="p">):</span>                       
    <span class="c1">#멤버 추가
</span>    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">cpu</span><span class="p">,</span> <span class="n">ram</span><span class="p">,</span> <span class="n">battery</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">(</span><span class="n">cpu</span><span class="p">,</span> <span class="n">ram</span><span class="p">)</span>            
        <span class="n">self</span><span class="p">.</span><span class="n">battery</span> <span class="o">=</span> <span class="n">battery</span>                
    <span class="c1">#메서드 추가
</span>    <span class="k">def</span> <span class="nf">move</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">to</span><span class="p">):</span>                       
        <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">move to {}</span><span class="sh">'</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="n">to</span><span class="p">))</span>                     
</code></pre></div></div> <p>컴퓨터 클래스는 인스턴스 멤버로 CPU와 RAM을, 인스턴스 메서드로 browse()와 work()를 가진다. 노트북 또한 동일한 요소를 가지고 기능을 할 수 있다. 이렇듯 <strong>어떤 객체나 다른 객체의 모든 특성(멤버)과 기능(메서드)을 가진 상태에서 그 외에 다른 특성이나 기능을 가지고 있다면 상속을 활용</strong>하는 것이 유용하다.<br/> Laptop 클래스의 이름 옆 괄호 안에는 Computer가 들어있다. 이는 Laptop 클래스가 Computer 클래스를 상속한다는 의미이다.<br/> super().__init__(cpu, ram) 문장은 Computer 클래스의 생성자를 호출하면서 여러개의 인자를 전달한다. 이 중 super는 현재 클래스의 슈퍼 클래스를 의미한다.</p> <h2 id="has-a-합성composition과-통합aggregation">HAS-A: 합성Composition과 통합Aggregation</h2> <p>HAS-A 관계는 ‘~이 ~을 가진다’를 의미한다. 컴퓨터는 CPU를 가지므로 ‘A Computer <strong> HAS-A</strong> CPU’가 성립하며, HAS-A 관계라고 부른다.<br/> HAS-A 관계는 합성과 통합으로 다시 나뉜다. 둘 모두 HAS-A 관계를 나타내는 방법이지만, 차이점이 존재한다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">CPU</span><span class="p">:</span>
    <span class="k">pass</span>

<span class="k">class</span> <span class="nc">RAM</span><span class="p">:</span>
    <span class="k">pass</span>

<span class="k">class</span> <span class="nc">Computer</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">cpu</span> <span class="o">=</span> <span class="nc">CPU</span><span class="p">()</span>   
        <span class="n">self</span><span class="p">.</span><span class="n">ram</span> <span class="o">=</span> <span class="nc">RAM</span><span class="p">()</span>
</code></pre></div></div> <p>Computer 클래스는 인스턴스 멤버 CPU를 가진다. 생성자에서 CPU 객체를 생성하여 멤버 cpu에 할당한다. 즉, Computer 객체와 CPU 객체는 함께 생성되고 사라진다. 따라서 객체의 생명주기가 같고, 어느 하나가 다른 하나를 소유하고 있는 강한 관계를 맺고 있을 때 <strong>합성Composition</strong>이라고 한다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Gun</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">kind</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">kind</span> <span class="o">=</span> <span class="n">kind</span>

    <span class="k">def</span> <span class="nf">bang</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">bang bang!</span><span class="sh">'</span><span class="p">)</span>

<span class="k">class</span> <span class="nc">Police</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">gun</span> <span class="o">=</span> <span class="bp">None</span>           <span class="c1">#1
</span>
    <span class="k">def</span> <span class="nf">acquire_gun</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">gun</span><span class="p">):</span>   <span class="c1">#2
</span>        <span class="n">self</span><span class="p">.</span><span class="n">gun</span> <span class="o">=</span> <span class="n">gun</span>

    <span class="k">def</span> <span class="nf">release_gun</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>        <span class="c1">#3
</span>        <span class="n">gun</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">gun</span>
        <span class="n">self</span><span class="p">.</span><span class="n">gun</span> <span class="o">=</span> <span class="bp">None</span>
        <span class="k">return</span> <span class="n">gun</span>
        
    <span class="k">def</span> <span class="nf">shoot</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">gun</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="n">gun</span><span class="p">.</span><span class="nf">bang</span><span class="p">()</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">Unable to shoot</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>#1에서 Police 객체가 만들어질 때 아직 Gun 객체를 가지고 있지 않다. #2의 acquire_gun() 메서드를 통해 Gun 객체를 멤버로 가지게 되며, #3의 release_gun() 메서드를 통해 총을 놓을 수 도 있다. 이 두 메서드를 이용해 총을 가진 경찰과 가지고 있지 않은 경찰 모두 표현할 수 있다.<br/> 정리하자면, 객체끼리 생명주기를 함께 하지 않는 상대적으로 약한 관계를 <strong>통합Aggregation</strong>이라고 한다.</p>]]></content><author><name></name></author><category term="CS"/><category term="Method_Overriding"/><category term="Polymorphism"/><summary type="html"><![CDATA[클래스]]></summary></entry><entry><title type="html">Gradient Method</title><link href="https://ottersem.github.io/blog/2024/Gradient-Method/" rel="alternate" type="text/html" title="Gradient Method"/><published>2024-09-10T12:15:00+00:00</published><updated>2024-09-10T12:15:00+00:00</updated><id>https://ottersem.github.io/blog/2024/Gradient-Method</id><content type="html" xml:base="https://ottersem.github.io/blog/2024/Gradient-Method/"><![CDATA[<h1 id="3경사법gradient-method">3경사법Gradient Method</h1> <p>학습 단계에서 손실 함수가 최솟값이 될 때의 매개변수 값을 찾아내는 방법은 매우 복잡하다. 또한 매개변수 공간이 매우 클 때 어디가 최솟값이 되는 곳인지 짐작하기도 어려운데, 이때 기울기를 이용해 함수의 최솟값을 찾으려는 것이 경사법이다. 다만 각 지점에서 함수의 값을 낮추는 방안을 제시하는 지표가 기울기인 것이지 기울기가 가리키는 곳에 정말 함수의 최솟값이 있는지, 정말로 나아가야 할 방향인지에 대해서는 보장할 수 없다.<br/> 경사법은 현 위치에서 기울어진 방향으로 일정 거리만큼 이동하고, 이동한 곳에서 기울기를 구하고, 그 기울어진 방향으로 나아가기를 반복한다. 이렇게 함수의 값을 점차 줄이는 것이 <strong>경사법</strong>이다.</p> <p>\(x_0 = x_0-\eta \frac{\partial f}{\partial x_0}\) \(x_1 = x_1-\eta \frac{\partial f}{\partial x_1}\)</p> <p>$\eta$(에타)는 갱신하는 양을 나타내며, 신경망 학습에서는 <strong>학습률</strong>이라고 한다. 한 번의 학습으로 얼마만큼 매개변수 값을 갱신하는지를 정하는 것이다. 위 수식을 바탕으로 경사 하강법을 구현하는 방법은 다음과 같다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">gradient_descent</span><span class="p">(</span><span class="n">f</span><span class="p">,</span> <span class="n">init_x</span><span class="p">,</span> <span class="n">lr</span><span class="o">=</span><span class="mf">0.01</span><span class="p">,</span> <span class="n">step_num</span> <span class="o">=</span> <span class="mi">100</span><span class="p">)</span> <span class="p">:</span>
    <span class="c1"># f:최적화 하려는 함수, init_x : 초기값, lr : learning rate(학습률), step_nump : 경사법에 따른 반복 횟수
</span>    <span class="n">x</span> <span class="o">=</span> <span class="n">init_x</span>

    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">step_num</span><span class="p">):</span>
        <span class="n">grad</span> <span class="o">=</span> <span class="nf">numerical_gradient</span><span class="p">(</span><span class="n">f</span><span class="p">,</span><span class="n">x</span><span class="p">)</span>
        <span class="n">x</span> <span class="o">-=</span> <span class="n">lr</span> <span class="o">*</span> <span class="n">grad</span>
    <span class="k">return</span> <span class="n">x</span>
</code></pre></div></div> <p>주의해야할 점은 학습률이 너무 크면 큰 값으로 발산하고, 반대로 너무 작으면 거의 갱신되지 않은 채 끝나기에 학습률과 같은 <strong>하이퍼 파라미터</strong>를 적절히 조정하는것이 필요하다.</p> <h2 id="신경망에서의-기울기">신경망에서의 기울기</h2> <p>신경망 학습에서도 당연히 기울기를 구해야 하고, 여기서 기울기는 가중치 매개변수에 대한 손실 함수의 기울기이다. 예를 들어 형상이 $2\times3$, 가중치가 $W$, 손실 함수가 $L$인 신경망의 경사는 $\frac{\partial L}{\partial W}$로 나타낼 수 있다. \(W= \begin{pmatrix} w_{11} &amp; w_{12} &amp; w_{13} \\ w_{21} &amp; w_{22} &amp; w_{23} \end{pmatrix}\) \(\frac{\partial L}{\partial W}= \begin{pmatrix} \frac{\partial L}{\partial W_{11}} &amp; \frac{\partial L}{\partial W_{12}} &amp; \frac{\partial L}{\partial W_{13}} \\ \frac{\partial L}{\partial W_{21}} &amp; \frac{\partial L}{\partial W_{22}} &amp; \frac{\partial L}{\partial W_{23}} \end{pmatrix}\) $\frac{\partial L}{\partial W}$의 각 원소는 각각의 원소에 대한 편미분이다.</p> <h2 id="학습-알고리즘">학습 알고리즘</h2> <p>신경망 학습의 절차는 다음과 같다.</p> <ol> <li>미니배치 : 훈련 데이터 중 일부를 무작위로 가져오고, 이 데이터의 손실 함수 값을 줄이는 것이 목표이다.</li> <li>기울기 산출 : 각 가중치 매개변수의 기울기를 구한다.</li> <li>매개변수 갱신 : 가중치 매개변수를 기울기 방향으로 갱신한다.</li> <li>반복 :1~3단계를 반복한다.</li> </ol> <p>이때 확률적으로 무작위로 골라낸 데이터인 미니배치로 데이터를 선정하기에 <strong>확률적 경사 하강법stochastic gradient descent, SGD</strong>라고 부른다.<br/> 학습을 반복함으로써 손실 함수의 값이 서서히 내려가는데, 이 결과만으로는 다른 데이터셋에도 비슷한 결과를 도출할지는 확신할 수 없다. 이렇게 훈련 데이터에 포함된 이미지만 제대로 구분하고, 그렇지 않은 이미지는 식별할 수 없는 <strong>오버피팅</strong>을 일으키지 않는지 확인해야 하는데, 그 단위로는 <strong>에폭epoch</strong>을 사용한다.<br/> 1에폭은 학습에서 훈련 데이터를 모두 소진했을 때의 횟수에 해당하며 1에폭별로 훈련 데이터와 시험데이터에 대한 정확도를 기록하는 것이 일반적이다.</p>]]></content><author><name></name></author><category term="DL"/><category term="Neural_Network,"/><category term="Gradient_Method"/><summary type="html"><![CDATA[뉴럴 네트워크에서의 경사법(Gradient Method)]]></summary></entry><entry><title type="html">NN Derivative</title><link href="https://ottersem.github.io/blog/2024/NN-Derivative/" rel="alternate" type="text/html" title="NN Derivative"/><published>2024-09-10T11:15:00+00:00</published><updated>2024-09-10T11:15:00+00:00</updated><id>https://ottersem.github.io/blog/2024/NN-Derivative</id><content type="html" xml:base="https://ottersem.github.io/blog/2024/NN-Derivative/"><![CDATA[<h1 id="미분">미분</h1> <p>미분은 한순간의 변화량을 표시한 것이다.</p> \[\frac{df(x)}{dx} = \lim\nolimits_{h \to 0} \frac{f(x+h) - f(x)}{h}\] <p>위 수식을 기반으로 미분을 파이썬으로 구현해보자.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">numerical_differentiation</span><span class="p">(</span><span class="n">f</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
	<span class="n">h</span> <span class="o">=</span> <span class="mf">10e-50</span>
	<span class="nf">return</span><span class="p">(</span><span class="nf">f</span><span class="p">(</span><span class="n">x</span><span class="o">+</span><span class="n">h</span><span class="p">)</span> <span class="o">-</span> <span class="nf">f</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="o">/</span> <span class="n">h</span>
</code></pre></div></div> <p>만약 h가 0 이라면 계산이 불가능하니 가능한 한 작은 값을 이용했다. 그러나 계산시에 작은 값이 생략되어 최종 계산 결과에 오차가 생기게하고 이 오차를 <strong>반올림 오차rounding error</strong>라고 한다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;&gt;&gt;</span> <span class="n">np</span><span class="p">.</span><span class="nf">float</span><span class="p">(</span><span class="mf">1e-50</span><span class="p">)</span>
<span class="mf">0.0</span>
</code></pre></div></div> <p>첫번째 개선점으로 아주 작은 값(h)를 $10^{-4}$를 이용하면 좋은 결과를 얻는다고 알려져 있다.<br/> 두번째 개선점은 <strong>차분</strong>과 관련한 것인데, 앞의 구현에서는 x+h와 x 사이의 함수 f의 차분을 계산하고 있지만 정확한 x위치의 기울기에 해당하지는 않기에 엄밀하게 일치하지는 않는다.<br/> 따라서 이 오차를 줄이기 위해 (x+h) 와 (x-h) 일 때의 함수 f의 차분을 계산하는 방법을 쓰기도 한다. 이 차분을 <strong>중심 차분</strong>이라고 한다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">numerical_differentiation</span><span class="p">(</span><span class="n">f</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
	<span class="n">h</span> <span class="o">=</span> <span class="mf">1e-4</span>
	<span class="nf">return</span><span class="p">(</span><span class="nf">f</span><span class="p">(</span><span class="n">x</span><span class="o">+</span><span class="n">h</span><span class="p">)</span> <span class="o">-</span> <span class="nf">f</span><span class="p">(</span><span class="n">x</span><span class="o">-</span><span class="n">h</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="mi">2</span><span class="o">*</span><span class="n">h</span><span class="p">)</span>
</code></pre></div></div> <h2 id="편미분">편미분</h2> <p>그렇다면 변수가 여러개인 함수는 어떻게 처리해야 할까? 다음 수식을 통해 살펴보자.</p> <p>\(f(x_0,x_1) = x_0^2 + x_1^2\)</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">function</span><span class="p">(</span><span class="n">x</span><span class="p">):</span>
	<span class="k">return</span> <span class="n">x</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span><span class="o">**</span><span class="mi">2</span> <span class="o">+</span> <span class="n">x</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span><span class="o">**</span><span class="mi">2</span>
</code></pre></div></div> <p>위 함수를 미분할 때에는 변수가 2개 이기에 어느 변수에 대한 미분인지를 구별해야 하고, 이와 같이 변수가 여럿인 함수에 대한 미분을 <strong>편미분</strong>이라고 한다. 편미분 수식은 $\frac{\partial f}{\partial x_0}$나 $\frac{\partial f}{\partial x_1}$ 처럼 작성한다.<br/> 그렇다면 동시에 편미분을 계산하고 싶다면 어떻게 할까? $(x_0, x_1)$ 양쪽의 편미분을 $(\frac{\partial f}{\partial x_0},\frac{\partial f}{\partial x_1})$과 같이 묶어서 계산하고, 벡터로 정리한 것을 <strong>기울기</strong>라고 한다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">numerical_gradient1d</span><span class="p">(</span><span class="n">f</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
    <span class="n">h</span> <span class="o">=</span> <span class="mf">1e-4</span>
    <span class="n">grad</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros_like</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="c1"># x와 형상이 같은 배열 생성
</span>
    <span class="k">for</span> <span class="n">idx</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">x</span><span class="p">.</span><span class="n">size</span><span class="p">):</span>
        <span class="n">tmp_val</span> <span class="o">=</span> <span class="n">x</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span>
        <span class="c1">#f(x+h)계산
</span>        <span class="n">x</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">tmp_val</span> <span class="o">+</span> <span class="n">h</span>
        <span class="n">fxh1</span> <span class="o">=</span> <span class="nf">f</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>

        <span class="c1">#f(x-h)계산
</span>        <span class="n">x</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">tmp_val</span> <span class="o">-</span> <span class="n">h</span>
        <span class="n">fxh2</span> <span class="o">=</span> <span class="nf">f</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>

        <span class="n">grad</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">=</span> <span class="p">(</span><span class="n">fxh1</span> <span class="o">-</span> <span class="n">fxh2</span><span class="p">)</span> <span class="o">/</span>  <span class="p">(</span><span class="mi">2</span><span class="o">*</span><span class="n">h</span><span class="p">)</span>
        <span class="n">x</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">tmp_val</span> <span class="c1">#값 복원
</span>    <span class="k">return</span> <span class="n">grad</span>
</code></pre></div></div> <p>이 기울기에 마이너스를 붙이면 기울기는 함수의 <strong>’가장 낮은 장소(최솟값)’</strong>을 가리킴을 알 수 있다. 더 정확히 표현하자면 <strong>기울기가 가리키는 쪽은 각 장소에서 함수의 출력 값을 가장 크게 줄이는 방향이다.</strong></p>]]></content><author><name></name></author><category term="DL"/><category term="Neural_Network"/><summary type="html"><![CDATA[뉴럴 네트워크에서의 미분]]></summary></entry><entry><title type="html">Method Overriding and Polymorphism</title><link href="https://ottersem.github.io/blog/2024/Method-Overriding-and-polymorphism/" rel="alternate" type="text/html" title="Method Overriding and Polymorphism"/><published>2024-09-08T17:45:00+00:00</published><updated>2024-09-08T17:45:00+00:00</updated><id>https://ottersem.github.io/blog/2024/Method-Overriding-and-polymorphism</id><content type="html" xml:base="https://ottersem.github.io/blog/2024/Method-Overriding-and-polymorphism/"><![CDATA[<h1 id="메서드-오버라이딩과-다형성">메서드 오버라이딩과 다형성</h1> <p><strong>다형성(polymorphism)</strong>이란 <strong>’상속 관계에 있는 다양한 클래스의 객체에서 같은 이름의 매서드를 호출할 때, 각 개체가 서로 다르게 구현된 메서드를 호출함으로써 서로 다른 행동, 기능, 결과를 가져오는 것’</strong>을 의미한다. 그리고 이것을 구현하기 위해 파생 클래스 안에서 상속받은 메서드를 다시 구현하는 것을 <strong>메서드 오버라이딩</strong>이라고 한다.</p> <h2 id="메서드-오버라이딩">메서드 오버라이딩</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">CarOwner</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">name</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">name</span> <span class="o">=</span> <span class="n">name</span>

    <span class="k">def</span> <span class="nf">concentrate</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">{} can not do anything else</span><span class="sh">'</span><span class="p">.</span><span class="nb">format</span>
              <span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">name</span><span class="p">))</span>

<span class="k">class</span> <span class="nc">Car</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">owner_name</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">owner</span> <span class="o">=</span> <span class="nc">CarOwner</span><span class="p">(</span><span class="n">owner_name</span><span class="p">)</span>
        
    <span class="k">def</span> <span class="nf">drive</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>                
        <span class="n">self</span><span class="p">.</span><span class="n">owner</span><span class="p">.</span><span class="nf">concentrate</span><span class="p">()</span>    
        <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">{} is driving now.</span><span class="sh">'</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">owner</span><span class="p">.</span><span class="n">name</span><span class="p">))</span>   

<span class="k">class</span> <span class="nc">SelfDrivingCar</span><span class="p">(</span><span class="n">Car</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">drive</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>                
        <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">Car is driving by itself</span><span class="sh">'</span><span class="p">)</span>   

</code></pre></div></div> <p>위 코드를 보면 car 객체는 CarOwner객체가 운전하며, drive() 메서드가 시작하면 운전중임을 나타낸다. car객체를 상속받은 파생 클래스 SelfDrivingCar는 자율주행차로 차 주인이 운전하지 않기에 상속받은 drive() 메서드가 불필요하지만, 나머지 멤버나 메서드는 유효하다. 이럴 경우 SelfDrivingCar 클래스는 car 클래스를 상속하지만 불필요한 drive() 메서드만 클래스 안에서 다시 정의하고 있다.<br/> 이렇게 파생 클래스에서 상속받은 메서드를 다시 구현하는 것을 <strong>메서드 오버라이딩(Method Overriding)</strong>라고 한다.<br/> 위 코드를 기반으로 각각의 drive() 메서드를 호출해도 구현 내용이 다르므로 결과는 다르게 나오고, 이처럼 같은 이름의 메서드를 호출해도 호출한 객체에 따라 다른 결과를 내는 것을 <strong>‘다형성’</strong>이라고 한다.</p> <h2 id="다형성">다형성</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Animal</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">eat</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>          <span class="c1">#1
</span>        <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">eat something</span><span class="sh">'</span><span class="p">)</span>

<span class="k">class</span> <span class="nc">Lion</span><span class="p">(</span><span class="n">Animal</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">eat</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>          <span class="c1">#2
</span>        <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">eat meat</span><span class="sh">'</span><span class="p">)</span>

<span class="k">class</span> <span class="nc">Deer</span><span class="p">(</span><span class="n">Animal</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">eat</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>          <span class="c1">#3
</span>        <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">eat grass</span><span class="sh">'</span><span class="p">)</span>

<span class="k">class</span> <span class="nc">Human</span><span class="p">(</span><span class="n">Animal</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">eat</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>          <span class="c1">#4
</span>        <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">eat meat and grass</span><span class="sh">'</span><span class="p">)</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="n">animals</span> <span class="o">=</span> <span class="p">[]</span>           <span class="c1">#5
</span>    <span class="n">animals</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nc">Lion</span><span class="p">())</span>
    <span class="n">animals</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nc">Deer</span><span class="p">())</span>
    <span class="n">animals</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nc">Human</span><span class="p">())</span>

    <span class="k">for</span> <span class="n">animal</span> <span class="ow">in</span> <span class="n">animals</span><span class="p">:</span>
        <span class="n">animal</span><span class="p">.</span><span class="nf">eat</span><span class="p">()</span>        <span class="c1">#6
</span></code></pre></div></div> <p>위 코드에서는 기본 클래스인 Animal에 eat()메서드가 있고 파생클래스에 오버라이딩 되어있다. 그러나 각각 육식, 초식, 잡식인지에 따라 무엇을 먹는지가 다르고, 호출시에도 오버라이딩 된 메서드를 호출한다. 이렇듯 불필요한 인스턴스가 생성되면 자원의 낭비로 이어지기에 애초에 Animal클래스의 인스턴스를 만들지 못하게 하고 싶다면 <strong>추상 클래스(abstract class)</strong>로 만들면 된다.<br/> 추상 클래스는 독자적으로 인스턴스를 만들 수 없고 함수의 몸체가 없는 <strong>추상 메서드</strong>를 하나 이상 가지고 있어야 한다. 또한 추상 클래스를 상속받는 파생 클래스 에서는 추상 메서드를 반드시 오버라이딩 해야한다. 그렇지 않으면 파생 클래스도 추상 클래서가 되어 인스턴스를 만들 수 없다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">abc</span> <span class="kn">import</span> <span class="o">*</span>                   <span class="c1">#1
</span>
<span class="k">class</span> <span class="nc">Animal</span><span class="p">(</span><span class="n">metaclass</span> <span class="o">=</span> <span class="n">ABCMeta</span><span class="p">):</span>  
    <span class="nd">@abstractmethod</span>
    <span class="k">def</span> <span class="nf">eat</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>                  <span class="c1">#2
</span>        <span class="k">pass</span>
</code></pre></div></div> <p>(#1)의 abc는 abstrct base class의 약자이다. 추상 메서드로 만들고 싶은 메서드 위에 데코레이터 abstractmethod를 붙여주고, 메서드 구현부를 pass해 함수 몸체를 비워두면 eat()메서드는 추상메서드가 되며, Animal 클래스를 상속받는 모든 파생 클래스는 내부에 eat() 메서드를 반드시 오버라이딩 해야한다.</p>]]></content><author><name></name></author><category term="CS"/><category term="Method_Overriding"/><category term="Polymorphism"/><summary type="html"><![CDATA[메서드 오버라이딩과 다형성(Polymorphism)]]></summary></entry><entry><title type="html">NN Learning</title><link href="https://ottersem.github.io/blog/2024/NN-Learning/" rel="alternate" type="text/html" title="NN Learning"/><published>2024-09-08T16:32:00+00:00</published><updated>2024-09-08T16:32:00+00:00</updated><id>https://ottersem.github.io/blog/2024/NN-Learning</id><content type="html" xml:base="https://ottersem.github.io/blog/2024/NN-Learning/"><![CDATA[<h1 id="학습">학습</h1> <p>신경망 학습에서 학습이란 훈련 데이터로부터 가중치 매개변수의 최적값을 자동으로 획득하는 것을 뜻한다. 학습의 지표중 하나인 <strong>손실 함수</strong>의 결과값을 가장 작게 만드는 가중치 매개변수를 찾는 것이 학습의 목표이고, 그 방법으로는 <strong>경사법</strong>을 사용한다.</p> <h2 id="데이터-주도-학습">데이터 주도 학습</h2> <p>기계학습의 중심에는 데이터가 존재한다. 데이터에서 답을 찾고 데이터에서 패턴을 발견하고 데이터로 무언갈 해내는 것이 기계학습이다. 그러나 보통 어떤 문제를 해결할 때 사람은 경험과 직관을 근거로 해답을 찾아내지만, 기계학습에서는 사람의 개입을 최소화 하고 데이터로부터 패턴을 찾으려 시도한다. 그런 방법의 하나로, 특징을 추출하고 그 패턴을 기계학습 기술로 학습하는 방법이 있다.<br/> 이와 같은 기계학습에서는 모아진 데이터로부터 규칙을 찾아내는 역할을 기계가 담당한다. 다만 여전히 ‘특징’은 사람이 설계하기에 문제에 적합한 특징을 설계하지 않으면 바라는 결과를 얻을 수 없다.<br/> 기계학습 문제는 데이터를 <strong>훈련 데이터</strong>와 <strong>시험 데이터</strong>로 나눠 학습과 시험을 수행하는 것이 일반적이다. 데이터를 이렇게 두 가지로 나누는 이유는 아직 보지 못한 데이터로도 문제를 올바르게 풀어내는 능력을 제대로 평가하기 위해 훈련 데이터와 시험 데이터로 데이터를 분리하는 것이다.</p> <h2 id="손실-함수">손실 함수</h2> <p>신경망 학습에서는 현재의 상태를 ‘하나의 지표’로 표현한다. 그리고 그 지표는 <strong>손실 함수loss function</strong>라고 한다.손실 함수는 일반적으로 <strong>오차제곱합</strong>과 <strong>교차 엔트로피 오차</strong>를 사용한다</p> <h3 id="오차제곱합sum-of-squares-for-error-sse">오차제곱합sum of squares for error ,SSE</h3> <p>오차제곱합의 수식은 다음과 같다. \(E = \frac{1}{2}\sum _k (y_k-t_k)^2\) 여기서 $y_k, t_k, k$는 각각 신경망의 출력, 정답 레이블, 데이터의 차원 수를 나타낸다. 오차제곱합은 각 원소의 출력과 정답 레이블의 차를 제곱한 후 그 총합을 구한다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">sum_squares_error</span><span class="p">(</span><span class="n">y</span><span class="p">,</span> <span class="n">t</span><span class="p">):</span>
	<span class="k">return</span> <span class="mf">0.5</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">((</span><span class="n">y</span><span class="o">-</span><span class="n">t</span><span class="p">)</span><span class="o">**</span><span class="mi">2</span><span class="p">)</span>
</code></pre></div></div> <h3 id="교차-엔트로피-오차cross-entropy-error-cee">교차 엔트로피 오차cross entropy error, CEE</h3> <p>교차 엔트로피 오차의 수식은 다음과 같다. \(E=-\sum_kt_k\log y_k\) $y_k, t_k$는 각각 신경망의 출력, 정답 레이블이다. 정답 레이블에서 정답에 해당하는 인덱스의 원소만 1이며, 나머지는 0이다(원-핫 인코딩). 따라서 교차 엔트로피 오차는 실질적으로 정답일 때의 추정($t_k$가 1 일때의 $y_k$)의 자연로그를 계산하는 식이 된다. 즉, 교차 엔트로피 오차는 정답일 때의 출력이 전체 값을 정하게 된다.<br/> 로그의 성질에 따라 정답에 해당하는 출력이 커질수록 0에 다가가다가, 그 출력이 1일때 0이 된다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">cross_entropy_error</span><span class="p">(</span><span class="n">y</span><span class="p">,</span> <span class="n">t</span><span class="p">):</span>
	<span class="n">delta</span> <span class="o">=</span> <span class="mf">1e-7</span>
	<span class="k">return</span> <span class="o">-</span><span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">t</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">y</span><span class="o">+</span><span class="n">delta</span><span class="p">))</span>
</code></pre></div></div> <h4 id="미니배치-학습">미니배치 학습</h4> <p>훈련 데이터에 대한 손실 함수의 값을 구하고, 그 값을 최대한 줄여주는 매개변수를 찾기 위해선 모든 훈련 데이터를 대상으로 손실 함수 값을 구해야 한다. \(E=-\frac{1}{2}\sum_n\sum_kt_{nk}\log y_{nk}\) 단순히 교차 엔트로피 오차의 범위를 N개의 데이터로 확장한 식이다. 다만 N으로 나누어 정규화 함으로써 평균 손실 함수를 구한다.<br/> 그러나 데이터의 수가 클 경우 데이터 일부를 추려 전체의 ‘근사치’로 이용하고 , 이 일부를 <strong>미니배치</strong>라고 한다. 또한, 이러한 학습 방법을 <strong>미니배치 학습</strong>이라고 한다.</p> <h3 id="손실-함수를-정의하는-이유">손실 함수를 정의하는 이유</h3> <p>머신러닝의 목표는 높은 ‘정확도’를 만들어 내는 매개변수를 찾는 것인데 어째서 정확도가 아닌 ‘손실 함수의 값’이라는 지표를 선택할까?<br/> 신경망 학습에서는 최적의 매개변수를 탐색할 때 손실 함수의 값을 가능한 한 작게 하는 매개변수 값을 찾는다. 이때 매개변수의 미분을 계산하고, 그 값을 단서로 매개변수의 값을 서서히 갱신하는 과정을 반복한다.<br/> 이때 그 가중치 매개변수의 손실 함수의 미분이란 <strong>가중치 매개변수의 값을 아주 조금 변화시켰을 때, 손실 함수가 어떻게 변하는가</strong>라는 의미이다. 다시 돌아와서 정확도를 지표로 삼아서는 안되는 이유는 <strong>미분 값이 대부분의 장소에서 0이 되기에 매개변수를 갱신할 수 없기 때문이다.</strong></p>]]></content><author><name></name></author><category term="DL"/><category term="Neural_Network"/><summary type="html"><![CDATA[뉴럴 네트워크에서의 학습]]></summary></entry><entry><title type="html">Object Oriented Programming</title><link href="https://ottersem.github.io/blog/2024/Object-Oriented-Programming/" rel="alternate" type="text/html" title="Object Oriented Programming"/><published>2024-09-06T12:27:00+00:00</published><updated>2024-09-06T12:27:00+00:00</updated><id>https://ottersem.github.io/blog/2024/Object-Oriented-Programming</id><content type="html" xml:base="https://ottersem.github.io/blog/2024/Object-Oriented-Programming/"><![CDATA[<h1 id="객체-지향-프로그래밍object-oriented">객체 지향 프로그래밍Object-Oriented</h1> <p>절차 지향과 달리 객체 지향은 ‘현실 세계에 존재하는 <strong>객체(object)</strong>를 어떻게 모델링 할 것인가?’에 대한 물음에서 출발한다.</p> <h2 id="캡슐화encapsulation">캡슐화Encapsulation</h2> <p>우리는 저마다의 특성을 기준으로 객체를 분류하거나 계층을 만들 수 있다. 승민, 지현, 현수는 모두 다른 사람이지만 ‘사람’이라는 계층(클래스)에 속하고 각각은 객체로서 남들과는 다른 특성을 가지고 있다.<br/> 또한 사람은 잠을 자거나 숨을 쉬기와 같은 행동을 할 수 있다. 이처럼 객체는 고유의 특성 값과 행동 혹은 기능으로 표현할 수 있다.<br/> 현실 세계의 객체를 나타내려면 변수와 함수만 있으면 된다. 현실 세계를 모델링하거나 프로그램을 구현하는데 이처럼 <strong>변수와 함수를 가진 객체를 이용하는 프로그래밍 패러다임을 ‘객체 지향 프로그래밍’</strong>이라고 하며, <strong>변수와 함수를 하나의 단위로 묶는 것을 ‘캡슐화’</strong>라고 한다.<br/> 위의 내용을 응용해서 사람과 사람 간에 돈을 주고받는 행위를 코드로 나타내보자.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">person_init</span><span class="p">(</span><span class="n">name</span><span class="p">,</span> <span class="n">money</span><span class="p">):</span>
	<span class="n">obj</span> <span class="o">=</span> <span class="p">{</span><span class="sh">'</span><span class="s">name</span><span class="sh">'</span> <span class="p">:</span> <span class="n">name</span><span class="p">,</span> <span class="sh">'</span><span class="s">money</span><span class="sh">'</span> <span class="p">:</span> <span class="n">money</span> <span class="p">}</span> 
	<span class="n">obj</span><span class="p">[</span><span class="sh">'</span><span class="s">give_money</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">person</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> 
	<span class="n">obj</span><span class="p">[</span><span class="sh">'</span><span class="s">get_money</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">person</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span>
	<span class="n">obj</span><span class="p">[</span><span class="sh">'</span><span class="s">show</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">perseon</span><span class="p">[</span><span class="mi">3</span><span class="p">]</span>
	<span class="k">return</span><span class="p">[</span><span class="n">obj</span><span class="p">]</span>

<span class="k">def</span> <span class="nf">give_money</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">other</span><span class="p">,</span> <span class="n">money</span><span class="p">):</span> <span class="c1">#1
</span>    <span class="n">self</span><span class="p">[</span><span class="sh">'</span><span class="s">money</span><span class="sh">'</span><span class="p">]</span> <span class="o">-=</span> <span class="n">money</span>
    <span class="n">other</span><span class="p">[</span><span class="sh">'</span><span class="s">get_money</span><span class="sh">'</span><span class="p">](</span><span class="n">other</span><span class="p">,</span> <span class="n">money</span><span class="p">)</span> <span class="c1">#2
</span>
<span class="k">def</span> <span class="nf">get_money</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">money</span><span class="p">):</span>
    <span class="n">self</span><span class="p">[</span><span class="sh">'</span><span class="s">money</span><span class="sh">'</span><span class="p">]</span> <span class="o">+=</span> <span class="n">money</span>

<span class="k">def</span> <span class="nf">show</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">{} : {}</span><span class="sh">'</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="n">self</span><span class="p">[</span><span class="sh">'</span><span class="s">name</span><span class="sh">'</span><span class="p">],</span> <span class="n">self</span><span class="p">[</span><span class="sh">'</span><span class="s">money</span><span class="sh">'</span><span class="p">]))</span>

<span class="n">Person</span> <span class="o">=</span> <span class="n">person_init</span><span class="p">,</span> <span class="n">give_money</span><span class="p">,</span> <span class="n">get_money</span><span class="p">,</span> <span class="n">show</span> <span class="c1">#3
</span>
<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="c1"># 객체 생성
</span>    <span class="n">g</span> <span class="o">=</span> <span class="n">Person</span><span class="p">[</span><span class="mi">0</span><span class="p">](</span><span class="sh">'</span><span class="s">greg</span><span class="sh">'</span><span class="p">,</span> <span class="mi">5000</span><span class="p">)</span>
    <span class="n">j</span> <span class="o">=</span> <span class="n">Person</span><span class="p">[</span><span class="mi">0</span><span class="p">](</span><span class="sh">'</span><span class="s">john</span><span class="sh">'</span><span class="p">,</span> <span class="mi">2000</span><span class="p">)</span>

    <span class="n">g</span><span class="p">[</span><span class="sh">'</span><span class="s">show</span><span class="sh">'</span><span class="p">](</span><span class="n">g</span><span class="p">)</span>
    <span class="n">j</span><span class="p">[</span><span class="sh">'</span><span class="s">show</span><span class="sh">'</span><span class="p">](</span><span class="n">j</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sh">''</span><span class="p">)</span>
    
    <span class="c1"># 메시지 패싱 
</span>    <span class="n">g</span><span class="p">[</span><span class="sh">'</span><span class="s">give_money</span><span class="sh">'</span><span class="p">](</span><span class="n">g</span><span class="p">,</span> <span class="n">j</span><span class="p">,</span> <span class="mi">2000</span><span class="p">)</span>  <span class="c1">#4
</span>    
    <span class="n">g</span><span class="p">[</span><span class="sh">'</span><span class="s">show</span><span class="sh">'</span><span class="p">](</span><span class="n">g</span><span class="p">)</span>
    <span class="n">j</span><span class="p">[</span><span class="sh">'</span><span class="s">show</span><span class="sh">'</span><span class="p">](</span><span class="n">j</span><span class="p">)</span>

<span class="p">(</span><span class="n">base</span><span class="p">)</span> <span class="n">greg</span> <span class="p">:</span> <span class="mi">5000</span>
	   <span class="n">john</span> <span class="p">:</span> <span class="mi">2000</span>

	   <span class="n">greg</span> <span class="p">:</span> <span class="mi">3000</span>
	   <span class="n">john</span> <span class="p">:</span> <span class="mi">4000</span>
</code></pre></div></div> <p>#1 아래의 세 함수 중 give_money() 함수는 두 객체 간에 상호작용이 일어나 각자가 가지고 있는 데이터를 변경하고, #2의 other 객체를 통해 돈을 받는 객체가 가진 특정 함수를 호출하여 변경한다. 이처럼 <strong>서로 다른 객체가 함수 호출을 통해 상호작용하여 객체의 상태가 변하는 것</strong>을 <strong>메시지 패싱(message passing)</strong>이라고 한다. 기억해야 할 부분은 서로 다른 객체가 상호작용할 때 함수를 호출했다는 것과 함수 안에서 상대의 변수를 바꾸려면 상대가 가진 특정 함수를 호출해야 한다는 점이다.<br/> #3는 단지 함수들을 튜플로 묶고있지만 클래스처럼 동작한다. 또한 #4에서 함수를 호출할 때 함수에 전달된 첫 인자가 호출한 객체 자신임을 인지하고 클래스를 사용해 객체를 만들어보자.</p> <h2 id="클래스class">클래스Class</h2> <p>클래스를 정의하면 다음과 같다.</p> <ol> <li>객체라는 메모리 공간을 할당 한 다음</li> <li>객체 안에 묶인 변수를 초기화하고</li> <li>함수를 호출하는 데 필요한 것 클래스는 객체를 생성해 내는 템플릿이고, 객체는 클래스를 이용해 만들어진 변수와 함수를 가진 메모리 공간이다.<br/> 객체와 유사한 개념으로는 <strong>인스턴스(instance)</strong>가 있다. 인스턴스는 이 객체가 어떤 클래서에서 만들어졌는지에 초점을 맞춘 용어이다.<br/> 이제 위에서 작성한 Person을 클래스를 이용해 만들어보자. <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Person</span><span class="p">:</span>                           <span class="c1">#1
</span> <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">name</span><span class="p">,</span> <span class="n">money</span><span class="p">):</span>    <span class="c1">#2
</span>     <span class="n">self</span><span class="p">.</span><span class="n">name</span> <span class="o">=</span> <span class="n">name</span>                <span class="c1">#3
</span>     <span class="n">self</span><span class="p">.</span><span class="n">money</span> <span class="o">=</span> <span class="n">money</span>
 <span class="k">def</span> <span class="nf">give_money</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">other</span><span class="p">,</span> <span class="n">money</span><span class="p">):</span> <span class="c1">#4
</span>     <span class="n">self</span><span class="p">.</span><span class="n">money</span> <span class="o">-=</span> <span class="n">money</span>
     <span class="n">other</span><span class="p">.</span><span class="nf">get_money</span><span class="p">(</span><span class="n">money</span><span class="p">)</span>
 <span class="k">def</span> <span class="nf">get_money</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">money</span><span class="p">):</span>
     <span class="n">self</span><span class="p">.</span><span class="n">money</span> <span class="o">+=</span> <span class="n">money</span>
 <span class="k">def</span> <span class="nf">show</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
     <span class="nf">print</span><span class="p">(</span><span class="sh">'</span><span class="s">{} : {}</span><span class="sh">'</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">name</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">money</span><span class="p">))</span>
</code></pre></div> </div> </li> <li>클래스로 묶이는 변수를 <strong>프로퍼티, 멤버 변수, 멤버</strong>라고 부르며 객체가 가지는 멤버를 <strong>인스턴스 멤버</strong>라고 부른다. 클래스에 묶이는 함수는 <strong>멤버 함수, 메서드</strong>라고 부르며 멤버와 메서드를 합쳐 <strong>‘속성(attribute)’</strong>이라고 부른다.</li> <li>클래스를 선언한다. 클래스 이름은 관용적으로 대문자로 작성한다.</li> <li>__init __() 함수는 생성자(constructor)이다. 언더바 두개는 <strong>’메직 메서드’</strong>라고 불리며, 클래스의 인스턴스 멤버가 생성될 때 실행하여 초기화하는 역할을 한다.</li> <li>self는 객체 자신을 의미하며, 생성 중인 객체에 name과 money라는 멤버를 만들고 전달받은 인자들로 할당한다. 해당 클래스를 이용해 돈을 전달하는 코드를 작성하면 다음과 같다. <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">__name__</span><span class="o">==</span><span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
 <span class="n">g</span> <span class="o">=</span> <span class="nc">Person</span><span class="p">(</span><span class="sh">'</span><span class="s">greg</span><span class="sh">'</span><span class="p">,</span> <span class="mi">5000</span><span class="p">)</span>          
 <span class="n">j</span> <span class="o">=</span> <span class="nc">Person</span><span class="p">(</span><span class="sh">'</span><span class="s">john</span><span class="sh">'</span><span class="p">,</span> <span class="mi">2000</span><span class="p">)</span>
    
 <span class="n">g</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span>
 <span class="n">j</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span>

 <span class="n">g</span><span class="p">.</span><span class="nf">give_money</span><span class="p">(</span><span class="n">j</span><span class="p">,</span> <span class="mi">2000</span><span class="p">)</span>             
 <span class="nf">print</span><span class="p">(</span><span class="sh">''</span><span class="p">)</span>
    
 <span class="n">g</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span>
 <span class="n">j</span><span class="p">.</span><span class="nf">show</span><span class="p">()</span>

 <span class="nf">print</span><span class="p">(</span><span class="n">g</span><span class="p">.</span><span class="n">give_money</span><span class="p">.</span><span class="n">__self__</span><span class="p">)</span>
</code></pre></div> </div> </li> </ol> <p>코드의 실행결과는 이전의 코드와 같다. 다만, 이전에는 함수를 호출할 때 첫 인자로 함수를 호출하는 객체를 전달했는데, 이번 코드에서는 전달하지 않았다. 인스턴스 메서드를 호출하면 객체가 자동으로 첫 번째 인자인 self로 객체 자신을 전달하기 때문이다.<br/> 위 코드에서 객체 g의 메서드를 살펴보자</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;&gt;&gt;</span><span class="nf">dir</span><span class="p">(</span><span class="n">g</span><span class="p">.</span><span class="n">give_money</span><span class="p">)</span><span class="c1">#1
</span><span class="o">&gt;&gt;&gt;</span><span class="n">g</span><span class="p">.</span><span class="n">givemoney</span><span class="p">.</span><span class="n">__func__</span><span class="c1">#2
</span><span class="o">&gt;&gt;&gt;</span><span class="n">g</span><span class="p">.</span><span class="n">givemoney</span><span class="p">.</span><span class="n">__self__</span><span class="c1">#3
</span></code></pre></div></div> <p>#1을 실행하면 객체 g의 give_money()의 메서드의 속성을 확인할 수 있다. 이 중 __func __를 확인해 보면 Person 클래스의 give_money() 함수임을, __self __를 확인해보면 Person 객체임을 알 수있다.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;&gt;&gt;</span><span class="n">g</span><span class="p">.</span><span class="n">givemoney</span><span class="p">.</span><span class="n">__self__</span> <span class="ow">is</span> <span class="n">g</span>
<span class="bp">True</span>
</code></pre></div></div> <p>또한 __self __가 이 메서드를 가진 객체 자신을 참조하고 있음을 확인할 수 있다.<br/> 따라서 <strong>메서드 내부에 함수와 객체의 참조를 가지고 있으므로 함수에 직접 객체의 참조를 전달 할 수 있기 때문에 객체에서 메서드를 호출할 때 self를 전달하지 않아도 된다.</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">A</span><span class="p">:</span>
    <span class="n">c_mem</span> <span class="o">=</span> <span class="mi">10</span>               <span class="c1">#1
</span>
    <span class="nd">@classmethod</span>
    <span class="k">def</span> <span class="nf">cls_f</span><span class="p">(</span><span class="n">cls</span><span class="p">):</span>          <span class="c1">#2
</span>        <span class="nf">print</span><span class="p">(</span><span class="n">cls</span><span class="p">.</span><span class="n">c_mem</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">num</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">i_mem</span> <span class="o">=</span> <span class="n">num</span>     <span class="c1">#3
</span>
    <span class="k">def</span> <span class="nf">ins_f</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="nf">print</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">i_mem</span><span class="p">)</span>
</code></pre></div></div> <p>#1 에서 c_mem과 같이 클래스가 가지는 멤버를 클래스 멤버라고한다. 또한 데코레이터@classmethod를 통해 cls_f() 메서드는 클래스 메서드가 된다.<br/> 클래스 멤버와 클래스 메서드는 클래스가 가지고 있으므로 객체가 없어도 클래스를 통해 접근하거나 호출할 수 있으며, 전역 변수와 전역 함수를 대체할 수 있다.</p> <h2 id="정보-은닉information-hiding">정보 은닉Information Hiding</h2> <p>캡슐화할 때 어떤 맴버와 메서드는 공개하여 유저가 사용할 수 있도록 하고, 어떤 것들은 숨겨서 유저가 접근할 수 없도록 할 것인지 정해야 하는데 이런 개념을 <strong>정보 은닉</strong>이라고 한다. 객체 지향 프로그래밍에서 잘된 정보 은닉은 필요한 메서드만 공개하고 나머지는 모두 숨기는 것이다. <br/> 파이썬은 기본적으로 정보 은닉을 지원하지 않기에 완벽한 정보 은닉은 불가능하다. 다만 유저의 실수를 막을 수 있는 두 가지 방법이 있다.</p> <ul> <li>숨기려는 멤버 앞에 언더바 붙이기</li> <li>프로퍼티 기법</li> </ul> <h3 id="언더바-붙이기">언더바 붙이기</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Account</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">name</span><span class="p">,</span> <span class="n">money</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">user</span> <span class="o">=</span> <span class="n">name</span>
        <span class="n">self</span><span class="p">.</span><span class="n">__balance</span> <span class="o">=</span> <span class="n">money</span><span class="c1">#1
</span>
    <span class="k">def</span> <span class="nf">get_balance</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">__balance</span><span class="c1">#2
</span>
    <span class="k">def</span> <span class="nf">set_balance</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">money</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">money</span> <span class="o">&lt;</span> <span class="mi">0</span><span class="p">:</span>
            <span class="k">return</span>

        <span class="n">self</span><span class="p">.</span><span class="n">__balance</span> <span class="o">=</span> <span class="n">money</span><span class="c1">#3
</span>
<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="n">my_acnt</span> <span class="o">=</span> <span class="nc">Account</span><span class="p">(</span><span class="sh">'</span><span class="s">greg</span><span class="sh">'</span><span class="p">,</span> <span class="mi">5000</span><span class="p">)</span>
    <span class="n">my_acnt</span><span class="p">.</span><span class="n">__balance</span> <span class="o">=</span> <span class="o">-</span><span class="mi">3000</span><span class="c1">#4
</span>
    <span class="nf">print</span><span class="p">(</span><span class="n">my_acnt</span><span class="p">.</span><span class="nf">get_balance</span><span class="p">())</span>
    
<span class="o">&gt;&gt;&gt;</span> <span class="n">my_acnt</span><span class="p">.</span><span class="n">__dict__</span>
<span class="p">{</span><span class="sh">'</span><span class="s">user</span><span class="sh">'</span><span class="p">:</span> <span class="sh">'</span><span class="s">greg</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">_Account__balance</span><span class="sh">'</span><span class="p">:</span> <span class="mi">5000</span><span class="p">,</span> <span class="sh">'</span><span class="s">__balance</span><span class="sh">'</span><span class="p">:</span> <span class="o">-</span><span class="mi">3000</span><span class="p">}</span>
</code></pre></div></div> <p>balance를 모두 __balance로 바꾸었다. 다만 실행 예와 같이 my_acnt의 멤버가 예상과는 다르다. 정보 은닉이 올바르게 되었다면 __balance가 보이지 않아야 하고, 멤버로 만든 적 없는 _Account__balance가 추가되어있다.<br/> 클래스 안에서 멤버 앞에 언더바를 두 개 붙이면 이 멤버는 객체가 만들어 질 때 ‘_클래스 이름’이 멤버 앞에 붙게된다. 다만 이렇게 변경된 멤버는 __dict__를 사용해 유저 또한 알고 수정할 수 있다. ‘__balance’ 멤버는 기존의 멤버 이름이 바뀌었기에 새로운 멤버 하나를 더 갖게 된것이다.</p> <h3 id="프로퍼티-기법">프로퍼티 기법</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Account</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">name</span><span class="p">,</span> <span class="n">money</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">user</span> <span class="o">=</span> <span class="n">name</span>
		<span class="c1">#인스턴스 멤버 선언이 아닌 #3의 setter 메서드 호출
</span>        <span class="n">self</span><span class="p">.</span><span class="n">balance</span> <span class="o">=</span> <span class="n">money</span><span class="c1">#1
</span>        
    <span class="nd">@property</span>
    <span class="k">def</span> <span class="nf">balance</span><span class="p">(</span><span class="n">self</span><span class="p">):</span><span class="c1">#2
</span>        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">_balance</span>

    <span class="nd">@balance.setter</span>
    <span class="k">def</span> <span class="nf">balance</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">money</span><span class="p">):</span><span class="c1">#3
</span>        <span class="k">if</span> <span class="n">money</span> <span class="o">&lt;</span> <span class="mi">0</span><span class="p">:</span>
            <span class="k">return</span>
		
		<span class="c1">#실제 인스턴스 멤버 선언이 일어나는 부분
</span>        <span class="n">self</span><span class="p">.</span><span class="n">_balance</span> <span class="o">=</span> <span class="n">money</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="n">my_acnt</span> <span class="o">=</span> <span class="nc">Account</span><span class="p">(</span><span class="sh">'</span><span class="s">greg</span><span class="sh">'</span><span class="p">,</span> <span class="mi">5000</span><span class="p">)</span>
    <span class="n">my_acnt</span><span class="p">.</span><span class="n">balance</span> <span class="o">=</span> <span class="o">-</span><span class="mi">3000</span><span class="c1">#4
</span>
    <span class="nf">print</span><span class="p">(</span><span class="n">my_acnt</span><span class="p">.</span><span class="n">balance</span><span class="p">)</span><span class="c1">#5
</span></code></pre></div></div> <p>#2에서 데코레이터 @property를 함수 정의에 붙였다. 따라서 balance() 메서드는 getter함수가 되어 함수 안의 코드를 실행하고 연산된 값을 반환한다. #3에 이름이 같지만 @balance.setter가 붙어있는 함수는 setter 함수의 역할을 하는데, 객체의 속성 값을 간접적으로 설정하고, 설정 시에 추가적인 검증이나 제약을 적용할 수 있다. 정리하자면 다음과 같다.</p> <ul> <li>@property : getter 메서드를 정의하기 위해 사용하며, 속성을 읽을때 이 메서드가 호출된다.</li> <li>@멤버이름.setter : setter 메서드를 정의할 때 사용되며, 속성에 값을 설정할 때 이 메서드가 호출된다. 하지만 이 방법도 원천적으로 유저가 접근하는 것을 막을수는 없다.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;&gt;&gt;</span> <span class="n">my_acnt</span><span class="p">.</span><span class="n">__dict__</span>
<span class="p">{</span><span class="sh">'</span><span class="s">user</span><span class="sh">'</span><span class="p">:</span> <span class="sh">'</span><span class="s">greg</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">_balance</span><span class="sh">'</span> <span class="p">:</span> <span class="mi">5000</span><span class="p">}</span>
<span class="o">&gt;&gt;&gt;</span> <span class="n">my_acnt</span><span class="p">.</span><span class="n">_balance</span> <span class="o">=</span> <span class="o">-</span><span class="mi">3000</span>
<span class="o">&gt;&gt;&gt;</span> <span class="n">my_acnt</span><span class="p">.</span><span class="n">balance</span>
<span class="o">-</span><span class="mi">3000</span>
</code></pre></div></div> <p>__dict __를 통해 멤버를 확인 할 수 있고, _balance 멤버로 접근해 값을 음수로 변경하면 setter 함수를 통해 변경하는 것이 아니므로 값이 음수로 바뀐다.</p>]]></content><author><name></name></author><category term="CS"/><category term="Object_Oriented_Programming"/><category term="OOP"/><summary type="html"><![CDATA[객체 지향 프로그래밍]]></summary></entry></feed>